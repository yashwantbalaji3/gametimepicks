# GameTime Picks — Engineering Handoff (2026-09-17, v1.3 TEAM + PLAYER RESEARCH, ~15:40 UTC)

Supersedes `HANDOFF-2026-09-17.md` (v1.2 history stays there). For the next session at
`/Users/yashwantbalaji/Downloads/gametimepicks`: read this file, `docs/TEAM_PLAYER_RESEARCH.md` (v1.3 contract +
runbook), `docs/GAMETIME_DATA_PLATFORM.md`, `docs/GAMETIME_LIVE.md`, `docs/RETENTION_ARCHITECTURE.md`, and memory
`gtp-v13-team-player-research`. Charter unchanged: no paid data, no new provider, never promote a
REJECTED/STOP/PAUSED model, never `git add -A`, merge never rebase, never commit `vp/**` / `HANDOFF-2026-08-20.md` /
`docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / pasted master prompts.

## ⭐ NEXT SESSION — START HERE (v1.4 Matchup Explorer + Compare — not started)

1. Print UTC + ET; `git fetch origin`; fast-forward (bots push `main` constantly).
2. Verify by behaviour: `/api/live/?sport=mlb` events · `?sport=nfl` `UNSUPPORTED_SPORT` · `/preview/live/` 404 ·
   `/teams/nfl/kansas-city-chiefs/`, `/players/nfl/keenan-allen/`, `/players/ufc/elizeu-zaleski-dos-santos/` 200 ·
   `/players/nfl/not-a-player/` 404 · sitemap has research URLs and no `[param]` paths.
3. From `app/`: `node scripts/data-platform/validate.mjs` · `node scripts/data-platform/build.mjs --all --check`
   (STALE after bot captures is normal → `--all`, `validate`, `parity`) · **then always**
   `node scripts/research/build-research-projections.mjs` and `--check`. Commit both stores with explicit paths.
4. Read `docs/TEAM_PLAYER_RESEARCH.md` §2–3, §6, §19 before designing v1.4.

## A. State

| item | value |
|---|---|
| shipped | `a1c62d74f` platform refresh · `735892c32` projection + guards · `6e34f82be` pages/SEO/build-cost fix · `4b16c95bb` discovery links · `60ac1ff25` docs |
| startup truth | 14:21Z: HEAD = origin = `34d3ea8dc` (FF from `7880fc2ab`); production `4581ab5c8`; last meaningful CI `35194704083` success on `2a60e7708` |
| CI | quality-gate **`35238483305` on `60ac1ff25` — success**, quality job 19m53s (v1.2: 22m41s) — unit 218 s · build 238 s (was 348) · rendered 233 s (was 128) · browser install 45 s · a11y 368 s (was 265) |
| gate (local) | lint 0 · tsc 0 · unit **6,664 / 6,663 / 0 fail / 1 skipped** (70 s) · build 0 (95.8 s) · post-build **566 / 563 / 0 fail / 3 skipped** (105 s) · route inventory **79 / 0 findings** · structural a11y 0 · browser a11y on research routes 24/24 (3 engines) |
| production | serves **`60ac1ff25` exactly**. Research routes 200 per sport, unknown slug 404, former EPL club 404; existing routes unchanged; MLB Live events; NFL `UNSUPPORTED_SPORT`; sitemap 1,426 URLs (1,388 research, 0 templates, 0 MLB players, 0 EPL teams); no internal strings; no Live/provider request from a research page |
| research receipts | P317 engine-level shadow ACCUMULATING 30/200 · P305-F EPL totals forward shadow not due (`forward-totals/` empty) · nfl-props-forward-shadow last success 09-16 · soccer-leagues / sport-schedules / soccer-dc-shadow failing on known ESPN HTTP 400 refusals — classified only, nothing adopted |
| verdict | **TEAM + PLAYER RESEARCH v1.3 — PUBLIC AND VERIFIED** |

## B. Commands

```bash
node scripts/research/build-research-projections.mjs            # write data/research-projection/v1 (≈2 s)
node scripts/research/build-research-projections.mjs --check    # exit 1 when stale vs the committed platform
npx tsx --test src/lib/research-pages/read-models.test.mjs src/lib/research-pages/boundary.test.mjs src/lib/research-pages/composition.test.mjs
npx tsx --test src/lib/research-pages/research-built.test.mjs   # after npm run build
```

## C. Coverage receipt (`data/research-projection/v1/readiness.json`)

| Sport | Team pages | Player/Fighter pages | Indexable | Noindex | Excluded |
|---|---:|---:|---:|---:|---:|
| MLB | 30 | 255 | 30 | 255 | 480 players |
| NFL | 32 | 750 | 653 | 129 | 3,274 players |
| EPL | 20 | 414 | 414 | 20 | 7 clubs · 1,151 players |
| UFC | — | 352 | 291 | 61 | 733 fighters |
| **Total** | **82** | **1,771** | **1,388** | **465** | |

## D. Decisions a later session must not undo silently

- `PLATFORM_CONSUMERS` (B1) = the projection builder + its verification test. Pages never import the platform.
- Refresh order platform → projection (RB3/RB4 fail otherwise).
- EPL `TEAM_RESULT_FAMILY` has no entry: no EPL record anywhere until an id-keyed score source exists.
- Follow scope unchanged (MLB/NFL teams, NFL players). No new Saved concept, localStorage key, Live poller or nav primary.
- `getOptimizerSettledDates()` memoized per production-build worker — do not remove without re-measuring build time.
- finals-history isolation allows 2023–25 gamePks only under `/teams/mlb/`.

## E. Defects found (classified)

| class | finding | disposition |
|---|---|---|
| operational (build cost) | shared `SlateStatusBar` → `getOptimizerSettledDates()` ≈740 ms on EVERY exported page | memoized per production build; build 162 s→96 s with 5× pages |
| product / SEO | sitemap listed 10 literal `[param]` template paths since P208 (404s) | families filtered; SM1 + RX2 |
| platform/projection (links) | MLB board `#game-<pk>` anchors render only on lean-bearing boards; first link rule linked schedule-only dates | rule tightened; RX4 crawls every research link + anchor |
| test/probe | no guard pinned the platform reader's foreign-schemaVersion refusal (probe 13b MISSED) | RB7 added; probe re-run CAUGHT |
| test/probe | own tests: leak needle tripped archive isolation scan; `\bAVG\b` with `/i` matched "avg (n)"; React `<!-- -->` split text | fixed |
| identity | ESPN has two athlete ids named Chris Manhertz | not merged; slug suffix rule gives both pages |
| performance | research entries pushed search index to 365 KB (budget 260) | only teams + indexable NFL players indexed (209 KB) |
| a11y | wide research tables lacked the repo's `data-scroll-x` scroller marker (reflow e2e) | marked; 24/24 |
| provider/business | ESPN HTTP 400 refusals (soccer, NFL/NBA schedule) | carried ops debt |
| P0/P1 | none |

## F. Remaining gaps

| Gap | Sport | User impact | Next program |
|---|---|---|---|
| zero-safe 2026 player box scores | NFL | no current-season logs | data program |
| retained dated results captures | NFL, UFC | some 2026 finals unrecorded | ops |
| id-keyed final scores | EPL | no records/results | data program (founder: source) |
| box scores / full player identity | MLB | captured categories only | data program |
| method/round source | UFC | outcome only | founder gate (licence) |
| scheduled platform + projection refresh | all | research ages until a human refreshes | ops decision |
| CI rendered phase +105 s | all | 5 min margin left under 25 min | optimize walkers before adding pages |

## G. v1.4 readiness

| Capability | Ready? | Missing |
|---|---|---|
| Team comparison | yes (MLB, NFL) | EPL results |
| Player comparison | partial | NFL/EPL yes on shared columns; MLB categories partial; UFC outcome-only |
| Head-to-head | yes (MLB, NFL via `listHeadToHead` finals) | EPL scores; needs a per-pair registry + budget |
| Season filtering | yes | — (canonical season ids in every projection) |
| Matchup Explorer | partial | per-matchup projection, route budget, forecast join per game |

**Next bounded program: v1.4 Matchup Explorer + Compare. Not started.**
