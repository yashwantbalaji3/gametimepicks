# ux/

UX strategy, audits, and design-system decisions. Holds page-by-page review tables, the nav/IA model, and prioritized UX fix lists.

Open UX themes (from the July-4 review): /results is 11MB (mobile drag), token split (`--vault` vs `--gtp`), a few duplicated formatters, /picks legacy card clutter. All cosmetic — none block launch, but they shape post-launch polish.
