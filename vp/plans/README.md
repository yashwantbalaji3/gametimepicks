# plans/

Implementation plans handed to **Claude Code**. Each plan is scoped, gated, and written so Code can execute without re-deriving intent. I write these; Code implements; nothing is "done" until the gates prove it.

Plan template: **goal · scope (in/out) · steps · acceptance criteria (which gates must pass) · risks · rollback**.

Naming: `NNNN-short-slug.md`. Start here: `NEXT-FOR-CLAUDE-CODE.md`.
