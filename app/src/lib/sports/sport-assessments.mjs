/**
 * Per-sport gate assessments — committed evidence, not optimism (Program 144 · Release H).
 *
 * Each entry cites the receipt that justifies its status. UNPROVEN needs no citation — it is the
 * default. These are INTERNAL maturity readings; the public display state is derived separately
 * (lib/home/simulation-hub.mjs) and can be more conservative — UFC is internally SCAFFOLDED (some
 * stages partially proven) while publicly HISTORICAL_ONLY, and both are correct on their own axis.
 *
 * Update an entry only with a new receipt: a merged commit, a green run, a settled backtest.
 */

export const SPORT_ASSESSMENTS = {
  mlb: {
    inSeason: true,
    historicalArchive: true,
    stages: {
      schedule: { status: "PROVEN", evidence: "MLB StatsAPI daily schedule; doubleheader-safe identity {away}-{home}-{date}-{gamePk}" },
      identity: { status: "PROVEN", evidence: "ESPN board ids + midfield headshots through the identity components (Release E guards)" },
      data: { status: "PROVEN", evidence: "daily pipeline inputs; pitcher-workload capture live since Program 096-099" },
      markets: { status: "PROVEN", evidence: "credit-guarded Odds ingestion, de-vig first (~1.069 implied sums, Sprint 046)" },
      model: { status: "PROVEN", evidence: "10k-run simulations from board projections (lib/mlb/full-game)" },
      calibration: { status: "PROVEN", evidence: "preregistered backtests RAN and their truth is recorded: no measurable edge (76fae758), blend w=0 (6fcef8fc); markets demoted to market-context — the calibration stage proves the CLAIM DISCIPLINE, not superiority" },
      qualification: { status: "PROVEN", evidence: "publication gate in board generation (Sprint 043); no-play first-class (Program 140)" },
      products: { status: "PROVEN", evidence: "daily-products.yml scheduled, money-guarded, content-idempotent (Program 140)" },
      publication: { status: "PROVEN", evidence: "product-state contract: freshness from the product's own artifact (Program 140)" },
      settlement: { status: "PROVEN", evidence: "official StatsAPI grading, lineage gate proven live (Sprint 049), one settlement writer" },
      monitoring: { status: "PROVEN", evidence: "evidence ledger + cron watchdog + ops alerting cover the MLB chain (Program 144-A)" },
      owner: { status: "PROVEN", evidence: "automation owns the daily run; founder escalation via ops webhook wiring" },
    },
  },

  nfl: {
    inSeason: true, // P171-F: the 2026 preseason window is live and captured daily
    historicalArchive: false,
    stages: {
      owner: { status: "PROVEN", evidence: "P185: lib/sports/sport-owners.mjs names nfl-event-window.yml as the accountable daily job, with nightly-settle.yml as the single settlement writer. Guard sport-owners.test.mjs asserts the workflow EXISTS, carries a cron (a dispatch-only job cannot own a daily run), and that failures reach a human through scripts/ops_alert.sh with OPS_WEBHOOK_URL — wired into this sport's own workflow on 2026-08-20, because a settler that alerts does not help a run that died before reaching it. An owner that cannot be paged is a label. The guard also pins that settlement has exactly ONE owner across all sports, since a per-sport settler would be a second writer to the money chain" },
      qualification: { status: "PROVEN", evidence: "P185: lib/sports/qualification.mjs states the shared ladder once and classifies each rung PLAY/NO_PLAY. Applied by code in lib/sports/nfl/shadow-run.mjs; the guard reads that module's own state literals and fails if registry and code disagree in EITHER direction. No-play is first-class and reachable — this sport implements 3 declining rungs (REFUSED_POST_START / ABSTAIN / READY_EXCEPT_ODDS), each stating WHY, so an abstention is auditable rather than a silent gap. The vocabulary is CLOSED: a rung emitted outside the shared ladder fails the guard, which is the check that caught MODELLED_EXPERIMENTAL being invented outside the coverage axis" },
      identity: { status: "PROVEN", evidence: "P169-A: durable-id player registry from the 32-team roster capture (2,991 players; ids /^\\d+$/, names never mint identity, same-capture cross-team duplicates quarantine both rows, effective-dated membership across captures). P171-D: the odds join resolves anytime-TD outcome labels through THAT registry or quarantines — a prop label cannot mint a player (guard: p171-authorization.test.mjs)" },
      markets: { status: "PROVEN", evidence: "P171-D: founder receipt (docs/receipts/ODDS_AUTHORIZATION_P171.md) parsed fail-closed; authorized captures live under .github/workflows/nfl-odds-capture.yml. Provider-verified opening usage from response headers (15,658 remaining), season-aware key plan (the provider splits NFL across americanfootball_nfl and _preseason — run 1 proved the regular key holds 0 current-window events), ONE bulk call per live key = 3 credits/slate. Aug 13: 9 events, 287 book-market rows joined, 11 books, de-vig proportional per book, 8 join quarantines (out-of-window events), reconciliation exact, cumulative 12 of 3,000. Player props probed and typed NO_MARKET — absence is evidence, not a retry target" },
      schedule: { status: "PARTIAL", evidence: "P148 first capture; CADENCE 2/2: scheduled runs 31396780843 (Aug 10) + 31500117960 (Aug 11) — run 1 proved semantic idempotency (only stamps moved), run 2 committed an intended content change. PARTIAL: reschedule/postponement lineage not yet exercised by reality" },
      data: { status: "PARTIAL", evidence: "Program 151: private research corpus — 1,001 finals across 2023-2025 (272×3 regular-season exact, 7 ties preserved, Pro Bowls quarantined, 21 counted keyless requests, manifest claims retrieval only; guard: lib/sports/research/nfl-research.test.mjs). PARTIAL: historical corpus, not live daily inputs" },
      model: { status: "PARTIAL", evidence: "Program 151: chronological baselines over 569 decisive games — Elo log loss 0.6415 / 64.5% vs coin ln(2)=0.6931 anchor; margin MAE 11.0; 2025 postseason HISTORICAL_REPLAY through the shared harness (10/13, byte-deterministic, evaluationEligible). The BAR a future NFL model must beat, not a model claim; market comparison unavailable and stated. P166-E: the baseline's Elo promoted to a pure cutoff-versioned strength state (params identical, leakage structural — the target game cannot contribute by construction) + real-event input assembly proving READY_EXCEPT_ODDS on the next scheduled game from committed artifacts, post-start refusal included. P167-E: v1 engine — analytical margin/total heads over the cutoff Elo (train 2023-24, HELD-OUT 2025: logLoss 0.6478 vs coin 0.6931, ECE 0.0446, margin MAE 10.26), preseason ABSTAINS by model card, shadow ladder REFUSED_POST_START>ABSTAIN>READY_EXCEPT_ODDS>CURRENT_PRE_EVENT live in tests over the real next event (data/internal/research/nfl/model-card-v1.json). P171-A/B/C: role shares (held-out 2025 TV 0.4637 beats every baseline), player-prop heads (rush_yds PUBLIC_ELIGIBLE by policy: cov80 0.741/ECE 0.030; pass_yds RESEARCH_ONLY on honest under-coverage), anytime-TD calibration (LL 0.5492 vs const-λ 0.5525, ECE 0.0148, n=3,570). PARTIAL: every one of those is evaluated on REGULAR-season history — the live window is preseason, where the team model abstains by policy and player markets stay ROLE_UNCERTAIN. Nothing is promoted on preseason evidence" },
      publication: { status: "PARTIAL", evidence: "P171-F: /nfl publishes the authorized market layer — per-game de-vigged consensus, book counts, absolute capture stamp — rendered ONLY for rows whose capture precedes their own kickoff, and only while the artifact exists. Freshness derives from the market artifact itself, never from a sibling or build time. PARTIAL: no MODEL layer is public (team sim PRIVATE_ONLY, players ROLE_UNCERTAIN/NO_MARKET), so the publication stage cannot be PROVEN on a price table alone" },
      settlement: { status: "PROVEN", evidence: "P161: FINAL-only contract (WIN/LOSS/PUSH/VOID, tie=PUSH explicit) validated against ALL 1,001 corpus finals incl. the 7 ties. PROVEN 2026-08-20: 16 forecasts settled across 4 preseason dates (2026-08-13/14/15/16), 15 decisive. Every graded event carries the three things this stage asks for. OFFICIAL RESULTS: integer scores graded into winner, margin, total and 80% interval coverage. LINEAGE STAMPS: each event carries receiptFile, model, modelVersion, inputHash, forecastGeneratedAt and a revisionChain, so a grade can be traced to the exact forecast revision that produced it. QUARANTINE ON AMBIGUITY: a FINAL without integer scores quarantines itself and never throws the slate (scripts/nfl/settle-nfl-experimental.mjs) — the same rule that correctly withheld CAR-ARI on Aug 7 for missing schedule lineage when capture began Aug 9. A marketBenchmark is graded the SAME way on the same events, so the model is never scored against a friendlier yardstick than the book. Settlement measures a DISTRIBUTION — winner correctness, margin/total error, coverage, Brier, log loss — and carries no W-L or ROI: the record is forecast quality, not money" },
    },
  },

  nba: {
    inSeason: false,
    historicalArchive: true,
    stages: {
      schedule: { status: "PARTIAL", evidence: "P150 first capture (42 events, stated partial calendar); CADENCE 2/2: scheduled runs 31396780843 (Aug 10) + 31500117960 (Aug 11) — run 2 captured the provider RELEASING more season (42→58 events, seasonTypes 1+2) — the partial-calendar design absorbed real growth correctly. PARTIAL: reschedule lineage unexercised" },
      data: { status: "PARTIAL", evidence: "Program 152: private corpus — 4,179 finals across 2023-24/2024-25/2025-26 (1,230×3 regular EXACT, cup-final own phase, play-in 6×3, both All-Star formats quarantined, 28 counted keyless requests incl. the leap-day the exactly-1230 refusal caught; guard: lib/sports/research/nba-research.test.mjs). PARTIAL: historical, not live daily inputs" },
      model: { status: "PARTIAL", evidence: "Program 152: chronological baselines over 2,643 games — Elo log loss 0.6161 / 65.8% vs coin ln(2) anchor; margin MAE 12.0; 91-game postseason HISTORICAL_REPLAY through the shared harness (54/91, frozen-state conservatism stated, byte-deterministic); model-card-v1 with limitations + activation OFF. The BAR a future NBA model must beat" },
      identity: { status: "PARTIAL", evidence: "ESPN athlete ids resolve through PlayerAvatar; team coverage unverified for a full slate" },
      settlement: { status: "PARTIAL", evidence: "P162: FINAL-only contract (tied finals QUARANTINE — impossible after OT) validated against all 4,179 corpus finals with zero moneyline pushes; keyless capture + id-join adapter (seasonType blend refused, neutral-site preserved, population-exact reconciliation) wired into sport-schedules. First real receipt = honest NO_RESULTS_YET off-season zero-row state; first join window = Oct 3 preseason per the committed schedule. PARTIAL: no forward final graded yet by definition" },
      calibration: { status: "BLOCKED_EXTERNAL", blocker: "MLB stopping rule (Program 058-061): model R&D suspended after w=0 three times — a new sport model needs the same preregistered bar and a founder decision to invest" },
    },
  },

  epl: {
    inSeason: true,
    historicalArchive: false,
    stages: {
      identity: { status: "PROVEN", evidence: "P185: the EPL identity adapter (epl-identity.ts, in the soccer lane) resolves clubs to canonical ids with named rejection codes. Proven on REAL committed captures, not fixtures (guard epl-identity-live.test.mjs): all 380 fixtures of the 2026-27 season capture resolve with ZERO rejections into 380 DISTINCT ids, and every derived id re-derives byte-identical to the id already on disk, so adapter and artifact cannot drift. The join is the real evidence: the authorized odds capture is written by a different script from a different provider and keys on eventId — every priced row lands on an existing fixture, 0 orphans. An unresolvable club is REJECTED (UNRESOLVED_CLUB) rather than passed through with the provider spelling, and no id is minted from it; a fixture with no kickoff is MISSING_KICKOFF, never assumed to be today" },
      data: { status: "PARTIAL", evidence: "P185: the private corpus (data/internal/research/epl/corpus-v1.json) carries 1,520 matches across 4 seasons (2022-23..2025-26) with exactly the fields fitPoisson consumes — home, away, ftHome, ftAway — so the model's input FIELDS are present and no paid call is involved. PARTIAL on CLUB coverage, measured not assumed (guard epl-data-coverage.test.mjs): 18 of the 20 clubs in 2026-27 appear in the corpus; Coventry City and Hull City are newly promoted with no top-flight history and run at the league-average cold-start, which predictFixture FLAGS via coldStart rather than hiding. Exposure is pinned: 2 of 10 matchweek-1 fixtures and 74 of 380 season-wide lean on that fallback. A fixture priced off a league-average stand-in is not a fixture whose inputs are present, so this becomes PROVEN only as those clubs accumulate real matches — never by widening what \"covered\" means. P189 (2026-08-21): THAT ACCUMULATION NOW HAPPENS. fitEplStrength read a static corpus ending 2026-05-24, so the promoted clubs would have run at the league-average stand-in all season however many matches they played — on 2026-08-21 that put Hull City at 42.2% at home to Manchester United against a market price of 10.6%. Settled results now accrete into a current-season corpus (scripts/epl/accrete-epl-corpus.mjs, in epl-settle) and the next forecast fits on them. The frozen base is never written to. Leakage is prevented by the fit's cutoff rather than by filtering on write, proven in both directions: at the cutoff base+current is indistinguishable from base alone at 1520 matches, one second later it is 1521 and the promoted club enters knownClubs" },
      owner: { status: "PROVEN", evidence: "P185: lib/sports/sport-owners.mjs names epl-matchweek.yml as the accountable daily job, with nightly-settle.yml as the single settlement writer. Guard sport-owners.test.mjs asserts the workflow EXISTS, carries a cron (a dispatch-only job cannot own a daily run), and that failures reach a human through scripts/ops_alert.sh with OPS_WEBHOOK_URL — wired into this sport's own workflow on 2026-08-20, because a settler that alerts does not help a run that died before reaching it. An owner that cannot be paged is a label. The guard also pins that settlement has exactly ONE owner across all sports, since a per-sport settler would be a second writer to the money chain" },
      calibration: { status: "UNPROVEN", evidence: "P189 (2026-08-21): the INSTRUMENT exists and is scheduled; the SAMPLE does not. lib/sports/epl/learning-report.mjs computes the model's mean log loss and Brier over the graded ledger AND the market's over exactly the same matches, from the de-vigged price recorded before kickoff. That comparison was previously impossible: the ledger held the model's probabilities and no baseline, so a model improving while still losing to the closing line would have looked like progress. The de-vigged set existed for a moment inside runEplShadow and was discarded; it is now persisted on the PRIVATE forecast row at forecast time, never re-derived later from whatever odds file is on disk — that would compare the model to a price that did not exist when it spoke. A STOPPING RULE is preregistered before any sample exists, so it cannot be adjusted to spare a result: under 30 paired matches NO verdict is issued (figures still reported, text states they support nothing); materially worse is STOP_AND_DEMOTE with an explicit \"do not tune and re-run\"; tracking without beating is NO_MEASURABLE_ADVANTAGE. The report FITS NOTHING and recommends nothing, enforced by a guard — a pass that measures and adjusts together always finds an adjustment that helps, because it scores the fit on the data that produced it. Guards pin the sign convention (negative delta = model ahead; one flip would report losing as winning), that unpriced matches are EXCLUDED rather than scored as market misses, and that both sides average over the same matches. Live: 1 graded, 0 paired, NO_PAIRED_MATCHES / NOT_YET_ASSESSABLE — proven green in CI. UNPROVEN and not promotable by engineering: it needs matches played." },
      qualification: { status: "PROVEN", evidence: "P185: lib/sports/qualification.mjs states the shared ladder once and classifies each rung PLAY/NO_PLAY. Applied by code in lib/sports/epl/shadow-run.mjs; the guard reads that module's own state literals and fails if registry and code disagree in EITHER direction. No-play is first-class and reachable — this sport implements 3 declining rungs (REFUSED_POST_START / ABSTAIN / READY_EXCEPT_ODDS), each stating WHY, so an abstention is auditable rather than a silent gap. The vocabulary is CLOSED: a rung emitted outside the shared ladder fails the guard, which is the check that caught MODELLED_EXPERIMENTAL being invented outside the coverage axis" },
      schedule: { status: "PARTIAL", evidence: "P149 first capture (380/380, dual-source membership); CADENCE 2/2: scheduled runs 31396780843 (Aug 10) + 31500117960 (Aug 11) — both runs re-captured 380/380 clean and DISCARDED the unchanged snapshot (snapshot-per-capture idempotency live twice). PARTIAL: postponement lineage unexercised until the season supplies one" },
      markets: { status: "PROVEN", evidence: "AUTHORIZED 2026-08-18 by a committed founder receipt: EPL only, h2h + totals, BULK endpoint only, 500-credit ceiling, its own ledger, cron gated. PROVEN 2026-08-20 by the first successful capture — 2 credits (2 markets, cumulative 2 of 500), 9 fixtures priced across 11 books, de-vigged across the WHOLE three-way set rather than treating a draw as a two-way complement. The prior text claimed \"NO capture has yet succeeded — cumulative spend is 0\", which had been false since 07:47Z that morning. Consumption verified, not assumed: runEplShadow moves all four opening fixtures from READY_EXCEPT_ODDS to CURRENT_PRE_EVENT with vig visible (impliedSum 1.048-1.052) and de-vigged outcomes summing to 1. That took a fix — the capture publishes a per-fixture consensus under `matchResult` while the shadow filtered on marketType, so NO fixture could price; proven to be shape and not staleness by running against odds one hour old. The consensus is labelled \"consensus(median of N books)\", never dressed up as a bookmaker that posted a price, and the RAW american price is re-de-vigged here so the capture's own noVig is never de-vigged twice" },
      model: { status: "PROVEN", evidence: "Poisson score-matrix team model (epl-poisson / strength-state), cross-language parity with the live lib at n=1140 logLoss 1.0017, draw ECE 0.0262, totals diagnostics; lineups NOT_REQUIRED_FOR_TEAM_V1 by design (data/internal/research/epl/model-card-v1.json). PROVEN 2026-08-20: the prior text held this PARTIAL because \"all 10 matchweek-1 fixtures READY_EXCEPT_ODDS\" — true then, false now. 9 of 10 opening fixtures produce per-event distributions (e.g. Arsenal v Coventry City H 0.691 / D 0.178 / A 0.130); the tenth returns no market from the provider and is listed READY_EXCEPT_ODDS with its reason, never dropped. PREGAME INPUTS ONLY: the strength fit takes an explicit cutoff at the run clock so it can never see a result from a match it is forecasting, and odds are refused at/after kickoff. A newly promoted club with no top-flight history is FLAGGED coldStart rather than given a fabricated strength. The forecast set REFUSES to persist if any row claims CURRENT_PRE_EVENT while carrying no probabilities — the first build did exactly that, publishing nine rows that asserted a prediction and contained none. publicActivation stays OFF on every row: this proves the model RUNS, not that it should be acted on" },
      settlement: { status: "PARTIAL", evidence: "Contract v1 validated on a full real season (P151); P154 adds the OPERATIONAL current-results path: capture script + honest PRESEASON/NO_RESULTS_YET/SOURCE_STALE states (fresh stamps, zero fabricated rows), canonical-identity join with exactly-once consumption + total quarantine (guard: epl-current-results.test.mjs), wired into sport-schedules so the first real FT flows without deployment. P189 (2026-08-21): THE FIRST REAL FT IS GRADED. Arsenal 3-0 Coventry City settled from the official ESPN box score into two append-only ledgers: the team forecast hit at probabilityOfActual 0.69144 (logLoss 0.368979, over-2.5 also correct), and 62 player rows graded with 18 VOIDED — a conditional row whose condition did not hold is never scored as a miss. Player logLoss 0.305282, brier 0.09298, 10.81 predicted scorers against 9 observed; the three actual scorers were among the model's four highest Arsenal probabilities. ONE match is not a validation and is not claimed as one. Three defects had to be fixed to get there and each is now guarded: deriveEventId built its time component by string surgery and ate the MINUTES when an ISO stamp carried no seconds, so the same kickoff spelled two ways produced two ids and the join quarantined a real result (it now parses to an instant and formats UTC to the minute, spelling-invariant by construction); the grader parsed the raw artifact instead of loadCurrentEplResults, the identity bridge that already existed; and nothing was scheduled to look after a match ended, since every epl-matchweek slot fires at or before kickoff (epl-settle.yml is new, nightly at 23:00 UTC, ESPN-only so it costs no credits, and CARRIES a grading refusal past its commit step so a broken join can never discard the capture the run just made). Both graders are idempotent — verified by re-running each to a clean NOTHING_NEW exit. STILL PARTIAL: every grade so far was produced by a manual invocation. epl-settle has never fired on its own schedule, so there is no cadence receipt, and one match is not a cadence. P163-J: corrections DETECTION instrument ready before opening day (epl-results-monitor on the shared core — SCORE_CORRECTION review-gated, receipt-before-latest-wins per the runbook). P162-E hardening: kickoff-move/alias-drift/non-final-with-scores/correction/duplicate cases pinned (epl-results-hardening) + corrections runbook committed (docs/EPL_CORRECTIONS_RUNBOOK.md, latest-wins policy + ledger-writer acceptance condition)" },
      products: { status: "PARTIAL", evidence: "P189 (2026-08-21): the PRODUCT-STATE CONTRACT half is now met and scheduled. scripts/epl/build-epl-lane-status.mjs runs inside epl-matchweek and publishes public/data/admin/epl-lane.json, deriving every field from a committed artifact — next kickoff cluster, per-artifact freshness from each artifact's OWN stamp (never a file mtime, which records when git touched it), lineup-slot coverage, the season credit projection, the graded record and the gate itself. A missing source renders UNKNOWN rather than green, because \"we could not read it\" and \"there is nothing\" are different facts and only one is a product state. It is INTERNAL_ADMIN and swept from the public export by build reference — verified absent from out/ — since declaring a lane's state is not a public claim. Seven guards, including one that fails if the artifact is generated but missing from the commit allowlist: generated-and-never-committed is the shape that produced a 62-hour freshness outage here. P189-B (2026-08-22): EPL IS NOW A LIVE LAB LANE, on the gate's own terms. lab-eligibility.mjs held a literal stub for this sport — prices returning zero with the comment \"no odds feed is ingested for EPL at all\" — true when written, false once the odds receipt was authorised and false again once the first match was graded. The gate was not being strict, it was blind. Connecting the sensor is not lowering a bar: EPL now passes or fails on the same three requirements as every other sport (real posted prices, a proven official settlement path, four independently-priced games) computed from disk every run, and closes itself when the evidence goes. It is LIVE and it opens the cross-sport stream. THE LADDER SELECTS ON PRICE, NEVER ON THE MODEL: the UFC ladder picks its model's side because that model passed its bar, and its own header warns against copying the wrong precedent — EPL's has passed nothing and read Hull City at 42.2% against a 10.6% price, so EPL follows MLB and takes the MARKET'S OWN FAVOURITE, with a guard failing if the builder so much as reads a forecast artifact. Every leg is settleable by gradeEplLeg, proven end to end through the identity bridge (match_result/home WIN, away LOSS, total_goals over 2.5 WIN). Bands come from the canonical bucket; 2 of 4 card today and the unreachable ones are reported with the prices they actually reached rather than widened into existence. STILL PARTIAL for one reason only: the ladder has never run on its own schedule, so there is no cadence receipt. Building it surfaced four defects — a settled result being overwritten with an earlier status, a settler that would have left every EPL leg pending forever, a collision key that could not tell two team-market legs apart, and a night-before slot that would have published an empty ladder — each now guarded. Blockers carry WHOSE MOVE it is: calibration-sample and cold-start-clubs are REALITY_GATED, not backlog — filing them as engineering would put them on a board where they would sit failing until matches were played." },
      publication: { status: "PROVEN", evidence: "P185 (2026-08-20): /epl derives its freshness from the forecast artifact's OWN stamp — the built export renders \"Generated 2026-08-20T23:21:41Z\", byte-identical to generatedAt in the EPL lane's own forecasts/latest.json artifact, never a build time or a hand-typed date. Coverage is published without rounding up: 9 rows CURRENT_PRE_EVENT with per-fixture distributions, and the 1 fixture the provider returned no market for is NAMED on the page (\"Not forecast: ... READY_EXCEPT_ODDS\") rather than dropped to make 9 of 10 look like 10 of 10. The limitation travels WITH the numbers in rendered words: \"not validated out of sample\" as the page heading, and \"no Premier League match has been graded under this model ... no track record to cite\" above the table — so publication cannot be read as a claim of accuracy. Newly-promoted clubs on the league-average baseline are flagged inline per row, not silently priced. PROMOTED ONLY AFTER two contradictions were removed, both of which had a public surface asserting the opposite of the page: the nav entry read \"Schedule · simulation pending\" and rendered ON /epl directly above that heading, and the shared schedule component hardcoded \"the sports that are simulated today are MLB and NFL\" while /simulate derived 1. The two guards that should have caught the nav state were passing VACUOUSLY — they matched the schedule-only sentence inside the shared component, which /epl reaches only on the FALSE branch of a ternary it passes true; both now assert against the built export. This stage is about the publication path only: calibration and settlement remain separate stages and are NOT implied by it. P189 UPDATE (2026-08-21): the track-record sentence was a HARD-CODED string reading \"No Premier League match has been graded under this model\" — true when written, false the night Arsenal v Coventry City settled, in an artifact a settler was quietly invalidating. It is now derived from the ledger and reads \"1 Premier League match has been graded ... far too few to support any accuracy claim\". The derivation keeps it honest in BOTH directions: it cannot understate a growing record, and it refuses to quote an accuracy figure at any sample size, because a hit rate over a handful of matches is noise with a percent sign on it. validation stays NOT_VALIDATED_OUT_OF_SAMPLE regardless of count — no number on that line is a validation. The page's unreadable-artifact FALLBACK carried the same false sentence and now says the record could not be read, which is a statement about our files rather than about the world. EXTENDED same day (P188): publication now covers a per-fixture report at /epl/match/[slug], statically generated for each priced fixture from the same artifact through one shared loader, so the hub, the fixture list and the report cannot disagree about a fixture. The reports publish what the engine already computed and had been discarding — the ten likeliest scorelines with the mass they account for, the goal-total distribution and its over/under ladder, each side's own goal curve, both-teams-to-score, clean sheets and the winning-margin distribution — every figure an exact sum over ONE Poisson matrix, which is why NO run count is quoted anywhere for this sport: nothing is sampled. The workflow's produced-artifact assertion was extended to the PUBLIC forecast file at the same time; it had checked only the private research copy, so a run that wrote one and not the other would have reported success while every reader-facing surface went stale." },
      monitoring: { status: "PARTIAL", evidence: "P188: EPL moved from UNWATCHED to WATCHED. The registry recorded \"alerted, but nothing watches whether the matchweek run happened at all\" — alerting covers a run that ran and broke, and nothing covered a run that never started, which matters because GitHub crons are best-effort and a morning cron has already silently skipped in this repo. cron-watchdog.yml now derives EPL matchday from the COMMITTED FIXTURE CAPTURE (never the weekday, which goes quiet in exactly the weeks a schedule is unusual), checks whether epl-matchweek ran, and dispatches + alerts when it did not. The decision is lib/sports/epl/matchday-watch.mjs with 8 guards covering the states that matter: a missed run dispatches, a quiet week stays SILENT (a watchdog that cries on non-matchdays gets muted, which is the same as not having one), a midweek fixture arms it identically to a Saturday, and a run that HAPPENED but left a 24h-old forecast is caught — because the failure here is never a job that fails, it is a job that succeeds and writes nothing. Executed against the real artifacts: 380 fixtures read, forecast stamp resolved, TOO_EARLY at 15.2h out. PARTIAL, NOT PROVEN, for two stated reasons: the watchdog has never actually fired for EPL, so there is no receipt; and the single 14:30 UTC slot cannot cover a cluster's EARLIEST kickoff when it precedes the slot (Saturday 11:30, Sunday 13:00) — recorded as residualGap rather than rounded away, since a sport flipping to watched must not become indistinguishable from MLB's daily coverage. PROVEN needs a slot per cluster and one observed dispatch. P189 (2026-08-21): THE SLOT-PER-CLUSTER HALF IS NOW MECHANICALLY PROVEN, and by measurement rather than by reading the cron list. lib/sports/epl/lineup-slot-coverage.mjs derives kickoff clusters from the COMMITTED FIXTURE CAPTURE and slots from the WORKFLOW'S OWN CRON LINES — never a second copy of the cadence kept beside it, which would drift from the real one silently — and reports the lead time of the closest preceding slot. Executed live: 8 clusters spanning 20 fixtures over the opening fortnight, every one served at a 1.0h lead, 0 uncovered matches. The guard counts MATCHES rather than clusters, because a single unserved 14:00 slot is three matches with no lineup-aware simulation and calling that \"one gap\" understates it. A slot at or after kickoff scores as no slot at all rather than a zero-hour one. This matters beyond monitoring: the player model answers a CONDITIONAL question until a lineup exists, and only the lineup-time run produces the allocation-based simulation whose shares sum to one — so an early-only cadence is not a late product, it is a different and weaker one. WHAT COVERAGE IS NOT: a cron is a request, not a guarantee. GitHub's scheduler has drifted over an hour in this repository, and a 1.0h lead is exactly one drift away from firing after kickoff, where the engine refuses it. Coverage is a precondition; the observed dispatch is still the missing receipt. The cron-slot watchdog was also reporting FIRING as WORKING — it asked whether a run existed and never how it ended, so it said OK through three consecutive nfl-event-window failures; it now reads conclusions and reports FAILING with the streak length, which is what would surface epl-settle rotting." },
    },
  },

  ufc: {
    inSeason: true,              // forward cards captured (P150) — next event Aug 11
    historicalArchive: true,
    stages: {
      owner: { status: "PROVEN", evidence: "P185: lib/sports/sport-owners.mjs names ufc-fight-week.yml as the accountable daily job, with nightly-settle.yml as the single settlement writer. Guard sport-owners.test.mjs asserts the workflow EXISTS, carries a cron (a dispatch-only job cannot own a daily run), and that failures reach a human through scripts/ops_alert.sh with OPS_WEBHOOK_URL — wired into this sport's own workflow on 2026-08-20, because a settler that alerts does not help a run that died before reaching it. An owner that cannot be paged is a label. The guard also pins that settlement has exactly ONE owner across all sports, since a per-sport settler would be a second writer to the money chain" },
      qualification: { status: "PROVEN", evidence: "P185: lib/sports/qualification.mjs states the shared ladder once and classifies each rung PLAY/NO_PLAY. Applied by code in lib/sports/ufc/shadow-run.mjs; the guard reads that module's own state literals and fails if registry and code disagree in EITHER direction. No-play is first-class and reachable — this sport implements 3 declining rungs (REFUSED_POST_START / ABSTAIN / READY_EXCEPT_ODDS), each stating WHY, so an abstention is auditable rather than a silent gap. The vocabulary is CLOSED: a rung emitted outside the shared ladder fails the guard, which is the check that caught MODELLED_EXPERIMENTAL being invented outside the coverage axis" },
      markets: { status: "PROVEN", evidence: "AUTHORIZED 2026-08-18 by a committed founder receipt (docs/receipts/ODDS_AUTHORIZATION_UFC.md): UFC/MMA only, h2h only, us region only, BULK endpoint only, 500-credit ceiling, its own ledger. Parsed fail-closed in BOTH directions — the NFL receipt cannot fund a fight card and this one cannot fund an NFL slate (guard lib/sports/odds/ufc-authorization.test.mjs, 11 checks incl. two-key widening, ceiling-crossing refusal, per-event-endpoint ban). PROVEN 2026-08-20 by run 32421589470 — the first run to satisfy this stage's ACTUAL proof, de-vigged implied probabilities rather than merely a successful call. Capture: 94 per-book h2h rows across 12 bouts from 8 bookmakers, 1 credit (cumulative 5 of 500). Consumption: the preflight read that snapshot and priced 6 bouts CURRENT_PRE_EVENT carrying impliedSum + noVig per bookmaker; 6 ABSTAIN on model grounds with stated reasons; 1 READY_EXCEPT_ODDS — Spivac vs Petrino, EXACTLY the bout the capture independently reported under unjoinedBouts. The two artifacts agree about which fight has no market, which is what separates a working join from one that merely returns rows. Until this run the de-vig path was reachable from nothing: capture green, code committed and unit-tested, and the only caller ever passing oddsSnapshot was a test — so oddsReady:true meant prices joined to bouts, never that probabilities existed" },
      schedule: { status: "PARTIAL", evidence: "P150 first forward capture; CADENCE 2/2: scheduled runs 31396780843 (Aug 10) + 31500117960 (Aug 11) — run 2 recorded REAL lineage on fight day: one bout ADDED (82→83, stable ids, zero swaps/removals). P162: lineage CLASSIFIER shipped (lib/sports/ufc/lineage — stable-id joins, basis-recorded corner comparison, cancellation only from status, duplicate-id refusal) and reproduces that real addition from the committed captures. P163-D post-card observation: Aug 11→12 diff = 66 bouts/14 events left the forward window (classified REMOVED with the never-inferred-cancellation note — window mechanics), 17 UNCHANGED, ZERO replacements/swaps, zero refusals — a valid no-change lineage receipt. PARTIAL: replacement/cancellation still unobserved by reality; next qualifying window = post-UFC-330 (Aug 15)" },
      identity: { status: "PARTIAL", evidence: "boutId join made rematch-safe (Program 058-061); Program 153 corpus proves id-based fighter identity end-to-end (1,046 fighters, zero name-joins, self-matchup refusal)" },
      publication: { status: "PARTIAL", evidence: "P190 (2026-08-22): /ufc rendered NO freshness stamp of any kind. It showed a card artifact and never told a reader when that artifact was produced, so a two-day-old read and a two-minute-old one were indistinguishable — and on the night this was found it was showing a card read on 08-20 beside a ladder built on 08-18. Stamps are now PER-ARTIFACT and byte-identical to each source's own field, never reformatted and never a build time: a statically exported page compiled at build time would otherwise render the compile clock as if it were knowledge. Per-artifact rather than one global figure, because this page reads several with genuinely different stamps and one number standing for all of them is a figure built for one scope reused for a broader claim. Guards assert the card's stamp and the ladder's appear verbatim in the BUILT export, that the two differ, and that the ladder shown belongs to the card on the page. PARTIAL, not PROVEN: the freshness half is done, but this stage also asks that limitations travel with the numbers, and /ufc has not been audited for that the way /epl was." },
      products: { status: "PARTIAL", evidence: "P190 (2026-08-22): BOTH HALVES WERE MISSING. build-ufc-ladder.mjs was in no workflow at all — the only ladder on disk was a hand-run from 2026-08-18 for an event on 2026-08-22 — and nothing published the lane's state. It also dated itself by the RUN day while its card's real date sat in card.event.slateDate two lines away, and that date is baked into every slipId and is how the settler looks a ladder up. Three dates were live for one set of cards: written 08-18, fighting 08-22, published 08-21 through a latest.json fallback. Ladder wired into ufc-fight-week, dated by its card, fallback closed, and cards now render on /ufc through a component shared with EPL whose selection sentence comes from the ARTIFACT so one sport can never be shown under another's claim. build-ufc-lane-status.mjs publishes the contract (INTERNAL_ADMIN, swept from the export, verified absent from out/), deriving next card, per-artifact freshness, whether the published ladder belongs to the card on the page, and whether the lab settler can grade this sport at all — the two conditions that made every UFC card ungradeable. Live: PUBLISHED_FOR_THIS_CARD, 3 of 4 bands, settler IN_SCOPE. PARTIAL: neither the ladder nor the contract has yet run on a scheduled trigger, so there is no cadence receipt." },
      monitoring: { status: "PARTIAL", evidence: "P190 (2026-08-22): the cron-slot watchdog covers ufc-fight-week (3 crons, 0 missed) and now reads run CONCLUSIONS rather than merely whether a run existed, so a job that fires and fails is reported FAILING instead of OK. ufc-fight-week passes OPS_WEBHOOK_URL to scripts/ops_alert.sh, and delivery through that path is proven. PARTIAL: nothing watches whether the ladder or the lane status went STALE — the failure that actually happened here was not a job that broke but a job that never existed, publishing a four-day-old ladder while every surface reported the sport live. The lane artifact now reports STALE_FOR_A_DIFFERENT_CARD, but no watchdog reads it yet." },
      settlement: { status: "PARTIAL", evidence: "ONE card settled from official results (2026-06-15). P162-I: repeatable bout_winner contract validated against ALL 1,716 corpus finals — the 25 DRAW_OR_NC bouts are exactly the quarantines (winner-only source cannot split draw from NC, so v1 refuses to guess; zero pushes by construction); method/round refuse as unsupported. P162-J: forward capture + id-join adapter LIVE — first real run captured 12 finals from the Aug 8 card and quarantined ALL for missing lineage (bout captures began Aug 10, forward-only) with reconciliation exact: the lineage rule proven on real data for the third sport. Tonight's card IS in the captures; the first joined finals arrive with the next cadence run. P163-D FIRST JOINED RESULTS (bounded post-card observation, Aug 12 01:54 UTC): all 5 Contender Series finals JOINED with lineage from the committed bout captures and graded through the contract; the 12 pre-capture finals stay quarantined; reconciliation exact (17 = 0 nonFinal + 5 joined + 12 quarantined). P163-I: corrections monitor shipped — OVERTURNED_RESULT (winner flip) and DECISION_CHANGE (decided↔draw/NC churn) review-gated with append-only receipts; tonight's five finals read as exactly five BECAME_FINAL from the committed capture. PARTIAL: scheduled-run receipt still unexercised; no real correction observed" },
      data: { status: "PARTIAL", evidence: "Program 153: private corpus — 1,716 final bouts on 160 cards (Aug 2023–Aug 2026), 25 draw/NC preserved, 0 quarantined; rate-limit receipt in the manifest (burst→400, resumable fetcher at 15s spacing); winner-only field limitation stated (guard: lib/sports/research/ufc-research.test.mjs). PARTIAL: historical, not live" },
      model: { status: "PARTIAL", evidence: "Program 153: abstaining Elo baseline — coverage 25.6% BY DESIGN (in-corpus history only, stated), Elo 0.6791 vs listing-order prior 0.6906 vs coin ln(2) over 289 covered bouts; last-card replay through the shared harness (5/12 covered, abstentions with reasons ON the artifact); model-card-v1 names the weak signal as the finding. The BAR a future fight model must beat; activation OFF. P167-F: the abstaining Elo promoted to the live adapter (coverage semantics preserved: 288/1133 covered, logLoss 0.6788, ECE 0.0157, abstention 843 SPARSE/2 IDLE), per-bout ladder with CARD_UNCERTAIN lineage gate; REAL UFC 330 preflight: 11 READY_EXCEPT_ODDS + 1 ABSTAIN(SPARSE) (data/internal/research/ufc/model-card-v2.json)" },
    },
  },
};
