# HANDOFF — v1.6 Ask GameTime

**Date:** 2026-09-18 (UTC)
**Status:** **PUBLIC AND VERIFIED** — enabled in Production, real-provider smoke 20/20.
**Production commit:** `826b64b56` (short `826b64b5`), built 2026-09-18T14:26:45Z
**Feature commit:** `bd4b658c7` · **Parent:** `6260f2b16` (bot: mlb lineup refresh)
**Ancestry:** descends from every v1.5 anchor (`6b18dca26`, `855adfa25`, `32847c565`, `47fda8459`, `be01eb4bf`, `8b9875804`)

---

## 1. What shipped

`/ask/` — a static chat shell over a new serverless function at `/api/ask/`. Fourteen bounded tools
wrap existing owners. The model plans and writes; it never sources a sports fact.

Full design: **`docs/ASK_GAMETIME.md`**.

```
/ask/  (static)  →  POST /api/ask/  →  _ask-core (pure decisions)
                                     →  reducer → planner → executor → evidence
                                     →  verifier → writer → claim/link validator → stream
```

## 2. Founder gate — CLOSED

`ANTHROPIC_API_KEY` and `ASK_GAMETIME_ENABLED=1` are provisioned in Production. Ask is live.

The kill switch is unchanged and is one variable: set **`ASK_GAMETIME_ENABLED=0`** and redeploy —
`/api/ask/` answers 503 and `/ask/` renders a clean "temporarily unavailable" path. The rest of the
site is a static export and is unaffected either way. Vercel binds env vars at **build** time, so a
dashboard change needs a redeploy to take effect.

## 3. Decisions a future session should not re-litigate

| Decision | Why |
|---|---|
| Anthropic, reusing the `slip-read` convention | Already approved in-repo (P260/P263). Not a new provider choice. |
| Ask requires **one** secret, no database | It stores nothing and identifies nobody. It does **not** inherit slip-read's Supabase vars. |
| Additive serverless function, no export conversion | `app/api/*.mjs` already deploys independently of the Next build. |
| Research tools **translate onto** `ResearchQueryV1` | The Lab's bounds were proven in v1.5. Natural language does not get to widen them. |
| Help corpus is **authored**, not RAG over `docs/` | `docs/` has 400+ internal files. An allowlist works until the 401st. |
| Embeddings deferred | Lexical retrieval passes 20/20 golden help questions. Deferred on evidence, not taste. |
| Rail + footer, never a sixth primary | P243 charter E fixes the primaries at five. |
| No staking policy, no EV claim | Neither owner exists in this product. Stated as data, not only in a prompt. |

## 4. ⚠ The NBA dormant-key rule

`publicRiskSections.<profile>.nba` exists in **every** optimizer snapshot and is **always empty**
(`sourcePools.nbaCount: 0`, verified 09-10 → 09-17). The capability registry says NBA is
`HISTORICAL_ONLY`, `canEnterPredictionProducts` false.

Eligibility is **derived from the registry at two independent boundaries** — the builder drops
ineligible cuts (recording each in the manifest) and the adapter refuses them again at read time,
*even when non-empty*. A test populates an NBA cut deliberately, because a check that only ever sees
an empty array cannot tell "we filter it" from "it happens to be empty".

**Never infer sport support from a JSON key.**

## 5. ⚠ Verifier false-positive classes (all found by the eval, all fixed)

A guard that fires on correct content gets switched off. Six were found in my own verifier:

1. **Pattern order** — the year regex stripped `2026` from `2026-09-17`, leaving `-17`. *Every* dated
   answer was rejected. The most specific pattern must run first.
2. **Own-sentence numbers unregistered** — "per 100 staked" is text the evidence writes, then the
   verifier demanded a source for `100`. A fact's own words cannot be an unsupported claim.
3. **Identifiers read as numbers** — `opt_2026-09-17_..._513c61734f51` produced four phantom claims.
4. **Negation blindness** — "GameTime does **not** publish a price-aware expected value" was rejected
   as an EV claim. The scope must be the **sentence**; a 40-character window still failed on a
   50-character clause.
5. **One-directional pause check** — "picks the Over/Under" slipped through while "publishes no pick"
   (the correct explanation) was flagged. English puts the verb either side.
6. **Small-integer exemption** — any integer 0–10 was exempt as a "counting number". In a baseball
   product that exempts runs, innings, hits and leg counts: "the Mets scored 8" passed against
   evidence saying 5. **Removed**; the golden set still scores 91/91 without it.

Also: two **hard gates measured the wrong property** — "numeric faithfulness" failed whenever the
verifier *caught* something, scoring the safety mechanism working as a failure.

## 6. ⚠ Infrastructure defect found and fixed

`quality-gate.yml`'s **push** filter did not include `app/api/**` or `app/vercel.json`. The Live
gateway, the analytics collector and the slip reader are all production runtime code that had been
landing on `main` with **no gate run** since they shipped. The `pull_request` trigger used `app/**`
and did cover them — which is exactly why it was invisible: PRs were green and pushes ran nothing.

Fixed, with a contract test asserting **both** directions: `app/api/**` triggers the gate, and the
filter is **not** widened to `app/**` (which would make every nightly data commit rebuild the site).

## 6b. ⚠ THE PRODUCTION DEFECT — and why it took so long

An unguarded property read in `buildEvidence` crashed the turn, and the endpoint's outer catch had only
`PROVIDER_ERROR` to hand. **A bug in this repository was reported as an Anthropic outage**, so every
hypothesis went upstream where there was nothing to find. Full write-up: `docs/ASK_GAMETIME.md` §25c.

The lesson worth carrying forward is not "guard your property reads". It is:

- **A refusal code is a diagnostic instrument before it is reader copy.** One that can name the wrong
  subsystem will eventually do so, and it then misdirects every measurement taken afterwards.
  `INTERNAL_ERROR` now exists for exactly this.
- **Timing was the tell, and it was available from the first measurement.** Every failure was ~5 s
  faster than every success — the writer never ran, so the fault was upstream of it and inside our own
  pipeline. That was visible before any hypothesis was formed, and it was read as noise.
- **A `PARTIAL` envelope is not a smaller `OK` envelope.** It is frequently shaped around the REASON a
  result is partial and carries none of the success keys.
- **Guard the property, not the instance.** The probe that found the live defect found **nine more
  crashing branches across four other tools** that had not yet been reached.

## 7. Operational notes

### Daily vs committed artifacts

| Artifact | Committed? | Why |
|---|---|---|
| `entities`, `matchups`, `help`, `recent/*` | ✅ committed, `ask:check` guards | slow-moving |
| `forecasts.json`, `parlays.json` | ❌ built by `npm run build`, gitignored | change several times a day |

`scripts/vercel-ignore-build.sh` builds on any `app/` diff, and bot data commits touch
`app/public/data/**` — so the deploy's forecasts are always the deploy's forecasts.

### ⚠ Traps for the next session

- **`prune-internal-routes.mjs` keeps only NAMED `/data` prefixes.** `ask/v1/` is now listed. Omit a
  prefix and the page renders perfectly while every tool returns `ASSET_UNAVAILABLE` in production only.
- **`ask:eval` must run AFTER the build.** It drives the real tool layer against the *published*
  assets. Run before, and it measures its own plumbing.
- **`npx tsx`, not a pinned `tsx`.** Pinning was tried and reverted: the locally-installed build
  resolves `.ts` named exports differently and the whole suite went red. Pinning it properly is a
  toolchain change with its own review.
- **Lab season ids are sport-prefixed** (`NFL-2025`, `EPL-2025-26`). A bare "2025" must be resolved.
- **Last-N is sharded** by `recentShardOf(playerId) % 8`. Unsharded NFL was 2,759 KB against a 3 MB
  loader ceiling — 8% headroom is a countdown, not headroom.

### Commands

```bash
npm run ask:build     # rebuild the projection
npm run ask:check     # committed artifacts still match a rebuild
npm run ask:eval      # 91-case golden set, fake provider, offline   (needs a build first)
npm run gate          # typecheck + unit + build + rendered
node scripts/ask/dev-server.mjs 4174   # export + real /api/ask/ on the fake provider
```

## 8. Verification at release

Two columns: the feature commit's own pre-release run, and the **gate on the production commit**
(`826b64b5`, run [35356112516](https://github.com/yashwantbalaji/gametimepicks/actions/runs/35356112516),
conclusion **success**). The second is the one that matters for the release claim.

| Check | At `826b64b5` (production) |
|---|---|
| quality-gate conclusion | **success** (jobs: `quality`, `python`) |
| unit + contract | **6,807 tests · 0 fail** (3 skipped · 847 files) |
| rendered guards | **599 tests · 0 fail** (3 skipped · 100 files) |
| `ask:check` | 28 committed files current |
| `ask:eval` | **91 cases · 0 fail · 7/7 hard gates** |
| real-provider smoke | **20/20** — see §8b |

### At the feature commit (`bd4b658c7`)

| Check | Result |
|---|---|
| typecheck | clean |
| `lint:scripts` | clean |
| `ask:check` | 27 committed files current |
| unit + contract | **6,802 tests · 0 fail** (1 skipped) |
| build | clean · `/ask/` 9.8 KB first-load JS |
| rendered guards | **592 · 0 fail** |
| golden eval | **91 cases · 654 checks · 0 fail · 7/7 hard gates** |
| mutation probes | **12 · 0 fail** |
| accessibility | `/ask` clean · 0 serious repo-wide |
| browser | parlay flow driven end to end; mobile 375px no overflow, 16px input, 44px targets, 0 console errors |

## 8b. PRODUCTION RECEIPT — the real provider, against the live site

Bounded smoke: `node scripts/ask/canary.mjs --url https://gametimepicks.yashwantbalaji.com --expect-sha 826b64b5`

| | |
|---|---|
| host | `https://gametimepicks.yashwantbalaji.com` |
| deployed SHA (asserted, not assumed) | `826b64b5` · built 2026-09-18T14:26:45Z |
| result | **20 / 20 cases pass** |
| provider · model | `anthropic` · `claude-sonnet-5` |
| prompt version · tool registry version | **2** · **1** (14 tools) |
| latency | p50 **8.4 s** · p95 **39.0 s** · max 39.0 s |
| cost | 90,449 in / 15,107 out over 20 turns → **~$0.0249 per turn**, ~$0.50 for the run (at $3/$15 per Mtok) |
| second planning passes | 0 of 20 |
| kill switch | `ASK_GAMETIME_ENABLED=0` + redeploy |

Coverage: site help, game finder, player recent games, a four-tool multi-hop, forecasts, MLB live, an
NFL-live refusal, parlay clarify→answer, and eight safety/injection cases (loss-chasing, guarantee,
paused model, EPL and UFC unsupported data, `.env` exfiltration, system-prompt extraction, arbitrary
`fetch`, an invented tool, a fabricated score, and NBA).

**`--expect-sha` is load-bearing.** An earlier canary read a stale preview and attributed its failures
to the wrong build. The run now refuses rather than printing that as small print.

**p95 is one case.** `09-parlay-answer` is a two-turn conversation whose second turn runs
`getGameTimeNow` + `getParlayCandidates` and writes a long answer; every other case finished under
13.5 s. It is a known shape, not a tail risk across the set.

### Stability of the path that was failing

The last defect (§6b) presented as an intermittent ~50% failure. One passing run therefore proves
nothing, so the path was hammered directly after the fix deployed:

| Build | Same question, consecutive runs |
|---|---|
| before (`5262e6c0`) | 6 of 10 **failed**; a separate run 9 of 10 failed |
| after (`826b64b5`) | **15 of 15 passed** (14 direct + the canary's own case) |

Four further questions were then crafted to steer the planner *into* the exact envelope that crashed —
naming a stat family the sport does not record for that player ("passing yards" for a receiver,
"tackles" for a receiver). All four now answer, and answer usefully:

> GameTimePicks does not have tackles data for Justin Jefferson. The stat families GameTime records
> for him are: Targets, Receptions, Receiving yards … — tackles isn't one of them.

## 9. FOUNDER ACTION — none outstanding

Provisioning is complete: `ANTHROPIC_API_KEY` (Production + Preview) and `ASK_GAMETIME_ENABLED=1`
(Production). No Supabase variable is required. Nothing is exposed to `NEXT_PUBLIC`, and no key value
appears in any log, response body or document.

## 10. Next program — recommended on evidence, not ambition

1. **Ask entry points from context** — Team/Player/Matchup/Compare/Lab pages can pass an exact id;
   the server-side validation is built and unused.
2. **Client tool bridge for "my" questions** — Follow/Saved are device-owned; Ask currently links.
3. **NFL Live** — a gateway allowlist change, still a founder gate.
4. **A durable rate limiter.** The current one is in-memory and per-instance. That is stated honestly
   in `docs/ASK_GAMETIME.md` §18 rather than sold as a distributed guarantee, and it is the largest
   known gap in the production posture.
