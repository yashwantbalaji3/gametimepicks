# GameTime Picks — Engineering Handoff (2026-09-16, MLB LIVE BETA PUBLIC, ~05:30 UTC)

Supersedes `HANDOFF-2026-09-15.md` (Phase 6). For the next session at
`/Users/yashwantbalaji/Downloads/gametimepicks`: read §A, §H (the queue), `docs/GAMETIME_LIVE.md`, and
the memory index (`~/.claude/projects/-Users-yashwantbalaji/memory/MEMORY.md`, especially
`gtp-phase6-public-beta`). Charter unchanged: no paid data, no wagers, no social posts, never promote
a REJECTED/STOP/PAUSED model, never `git add -A`, merge never rebase, batch `app/` pushes, research
outside `app/`, never commit `vp/**` / `HANDOFF-2026-08-20.md` /
`docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / the pasted master prompts.

## A. State

| item | value |
|---|---|
| v1.0 | **unchanged and intact.** No public surface, route, artifact or model was modified this session |
| pushed | `ce91fe8d2` Live Stage 1 · `9bd27760c` handoff (root-only) · `a790ce481` MLB-deferral rationale |
| startup truth | production served `a9eefc4b8`; `git merge-base --is-ancestor 5d865dd68 a9eefc4b8` **true** |
| CI | quality-gate **`35050783964` on `a790ce481` — completed success** (python + quality both green). ⚠ The earlier run `35050327369` on `ce91fe8d2` reads **cancelled**: the workflow declares `concurrency: quality-gate-${{ github.ref }}` with `cancel-in-progress: true`, so pushing `a790ce481` superseded it. It is not a failure, and `ce91fe8d2` never got its own verdict — `a790ce481` is a superset and is the one that passed |
| production | serves **`a790ce481` exactly** (`build-info.json` builtAt 2026-09-16T03:11:31Z) |
| gate (local, on HEAD's tree) | lint **0** · unit **6,338 tests / 6,337 pass / 0 fail / 1 skipped** · tsc **0** · build **0** · post-build **546 / 543 pass / 0 fail / 3 skipped** · route inventory **74 routes, 0 findings (P0 0)** |
| flags | `LIVE_GATEWAY_ENABLED` (server) and `NEXT_PUBLIC_LIVE_ENABLED` (build) — **both OFF**. The production export contains **0** files referencing `/api/live` or live copy |
| production flag-off, verified 03:29Z | `/api/live/?sport=mlb` → `{"unavailable":true,"reason":"FEATURE_DISABLED"}` (deployed, makes no upstream call) · `/preview/live/` → **404** · `/`, `/nfl/`, `/mlb/`, `/today/`, `/build/custom/` → **200** |
| verdict | **MLB LIVE BETA — PUBLIC AND VERIFIED** (NFL Live remains internal) |

## A0. 🟢 MLB LIVE BETA — PUBLIC AND VERIFIED

**Activated 2026-09-16T05:03:35Z**, production build `7190ee9b9`. Verified 05:14–05:30Z. All twelve
founder checks pass; evidence table in `docs/GAMETIME_LIVE.md` §6a.

| | |
|---|---|
| gateway | `/api/live/?sport=mlb` → **15 events** (14 FINAL, 1 LIVE), 7,841 B |
| NFL | **`UNSUPPORTED_SPORT`** on every shape tried (`nfl`, `NFL`, `+event`, `+players&date`); `nba`/`epl` likewise |
| surface | `/games/mlb/[gameId]` renders **"Live game state · LIVE BETA"** with `Live now` and `Pregame GameTime · frozen` as separate aria regions |
| totals | PAUSED — `totalRuns`/`over-under` absent from the chunk **and** the rendered module |
| player UI | none — no "pregame range" row, no "on pace" |
| terminal stop | **1 request in 58 s** on a FINAL game |
| freshness | aged "5 sec" → "1 min" while hidden instead of claiming currency |
| CDN | 6 rapid requests → **1 MISS, 5 HITs**; one origin invocation serves many readers |
| v1.0 | 13/13 routes **200** · `/preview/live` 404 · `/live` 404 · 0 nav entries |
| QA | 375/390/768/1024 clean, **no console errors** |
| upstream | `statsapi.mlb.com` only |

⚠ **No monthly Vercel cost is claimed** — the sample is one late-evening slate, nearly all final. It
does not support a monthly figure. Re-measure on a full daytime slate.

## A1. ⚠ The activation blocker, and the fix (`7190ee9b9`)

Setting the env vars was not enough, and a dashboard redeploy did not finish the job.

**Vercel binds environment variables at BUILD time**, and `vercel-ignore-build.sh` skips builds with
no `app/` diff. So the same-commit redeploy bound the *function* env (the gateway came alive) but
**reused the static export** — leaving `NEXT_PUBLIC_LIVE_ENABLED` un-inlined and the UI dark. The
deployment was green and half-delivered.

Diagnosed, not guessed: the deployed chunk still contained the literal string
`NEXT_PUBLIC_LIVE_ENABLED` (Next leaves a runtime lookup when the var is absent at build time, and
the browser resolves it against an empty `process.env` polyfill → permanently `undefined`). After the
rebuild the name is **gone** from chunk `2318-419b313497cd487e.js` — inlined.

**Fix:** `VERCEL_FORCE_BUILD=1` in `vercel-ignore-build.sh` builds regardless of the diff. Strictly
opt-in (only `"1"`), can only ever cause more building than before, and pinned in the existing
behaviour suite — including a probe that the hatch actually CHANGES the outcome, and that `""`, `0`,
`false`, `true`, `yes` leave the logic untouched. **Use it for any future env-var-only activation.**

## A2. Stage 2 — MLB-only public Live beta (founder decision 2026-09-16)

**Decision:** proceed with MLB Live publicly; keep the ESPN-backed NFL surface internal until its
provider posture is separately approved. NFL Live is **preserved in full** and still under test.

| item | value |
|---|---|
| pushed | `62c6b35fd` — Stage 2 (one batch) |
| surface | `/games/mlb/[gameId]` — both the simulated and non-simulated render paths |
| label | **"Live beta"**, on a heading strip above two separately labelled regions |
| gate | lint 0 · unit **6,353 / 6,352 pass / 0 fail** · tsc 0 · build 0 · post-build **552 / 549 pass / 0 fail** · route inventory **74 routes, 0 findings (P0 0)** |
| live tests | **81**, all deterministic (committed fixtures + injected clocks) |
| CI | quality-gate **`35055009190` on `62c6b35fd` — completed success** (quality + python) |
| production | serves **`62c6b35fd` exactly** (`build-info.json` builtAt 2026-09-16T04:18:30Z) |
| ⚠ production behaviour | **UNCHANGED until the founder sets two env vars** — see §A3 |

### Production verification, 04:40Z

| check | result |
|---|---|
| `/api/live/?sport=mlb` | `{"unavailable":true,"reason":"FEATURE_DISABLED"}` |
| `/api/live/?sport=nfl&event=…` | `{"unavailable":true,"reason":"FEATURE_DISABLED"}` |
| `/games/mlb/sea-vs-laa-2026-09-15/` | **200** |
| `/`, `/mlb/`, `/nfl/`, `/today/`, `/build/custom/` | **200** |
| `/preview/live/` | **404** (internal, pruned) |

⚠ **Read that NFL row precisely.** It says `FEATURE_DISABLED`, not `UNSUPPORTED_SPORT`, because the
kill switch is evaluated BEFORE the sport allowlist — by design, and pinned by `ROLLOUT 5`. So this
production probe does **not** yet demonstrate the MLB-vs-NFL distinction; it demonstrates that
nothing is live at all. The allowlist itself is proven against the real handler
(`sport=nfl` → `UNSUPPORTED_SPORT`, `sport=mlb` → a real slate, in the same process) and becomes
observable in production the moment `LIVE_GATEWAY_ENABLED=1` is set. **Re-run this probe then**: MLB
must return events and NFL must return `UNSUPPORTED_SPORT`.

### Capability and permission are now DIFFERENT lists

`SUPPORTED_SPORTS` says an adapter exists. `LIVE_PUBLIC_SPORTS` (**default `mlb`**) says what this
deployment may call. The gateway refuses `sport=nfl` with `UNSUPPORTED_SPORT` **before a socket is
opened** — verified against the real handler — so a hand-crafted `/api/live?sport=nfl` in production
contacts nobody. The client half (`NEXT_PUBLIC_LIVE_SPORTS`, also defaulting to `mlb`) only stops us
shipping a panel that would be refused; **the server allowlist is the gate that holds.** Every
control defaults closed: a forgotten variable yields less capability, never more.

⚠ **Honest scope note.** A few NFL *constants* from the shared join module do ship (market keys, the
`espn-public` source label) because `LivePanel` serves both sports. They are inert strings — no URL,
no adapter, no capability. A built-export guard proves ESPN's own vocabulary (`STATUS_RAIN_DELAY`,
`shortDownDistanceText`, …) appears in **no** shipped file and that no shipped **script** contains a
provider host.

## A3. ⚠ THE ONLY REMAINING STEP — two Vercel environment variables

There is no Vercel CLI in this environment, so I could not set these. Full table in
`docs/GAMETIME_LIVE.md` §7.

```
LIVE_GATEWAY_ENABLED=1        # function — without it every request answers FEATURE_DISABLED
NEXT_PUBLIC_LIVE_ENABLED=1    # build    — without it LivePanel renders null
# leave LIVE_PUBLIC_SPORTS and NEXT_PUBLIC_LIVE_SPORTS UNSET — unset means MLB only
```

Rollback: unset `LIVE_GATEWAY_ENABLED`. Effective next invocation, no rebuild, no model artifact
regenerated.

## A4. Measured cost (measured, not estimated)

Instrumented against the real handler on a live 15-game slate:

| measurement | value |
|---|---|
| upstream calls for a whole slate | **1** (89,785 B) |
| served to a reader | **7,948 B** (11.3× reduction) |
| per-game reader payload | **665 B** |
| 10 different games viewed — before the memo | **10 calls, 898,700 B** |
| 10 different games viewed — after the memo | **0 calls, 0 B** |
| FINAL games (13 of 15) | TTL 3600 s, `clientIntervalMs: null` — **polling stops** |
| LIVE games (2 of 15) | TTL 25 s, poll 30 s |
| hosts contacted | `statsapi.mlb.com` only — **zero ESPN** |

⚠ **The memo exists because measurement found the defect.** The CDN caches by REQUEST url and every
MLB game has its own, so a CDN-only design pulled the full slate once per game per TTL. The memo
carries the **payload's own** `fetchedAt`, never a fresh one — refreshing it would hand an older
observation a fresh-looking age and the badge would lie in exactly the way Rule B exists to prevent.

## B. What was built

`docs/GAMETIME_LIVE.md` is the single document (architecture · lifecycle · schema · provider matrix ·
identity · cost · runbook). Summary:

- **Runtime — nothing new was needed.** Project-root `app/api/*.mjs` are Vercel functions that
  already run beside `output: "export"` (the Next app lives in `src/`). Proven in production:
  `/api/collect/` → 204, `/api/zzz-not-real/` → 404. Live is one more such function, `/api/live`.
- **Contract** `lib/live/contract.mjs` (v1) — node-free, shared by the function and the browser. No
  component ever sees a provider field name; the adapters are pure parsers with no `fetch` and no URL
  in executable code (asserted).
- **Adapters** `lib/live/adapters/{mlb-statsapi,espn-nfl}.mjs`.
- **Gateway** `api/live.mjs` + `api/_live-core.mjs` (all decisions, no I/O, unit-tested).
- **UI** `components/live/{live-panel,live-primitives,use-live-event}` — one visual language, one
  polling loop.
- **Preview** `/preview/live` — internal, `guardInternalRoute()`, pruned from the export (asserted
  against the built `out/`, not merely against the guard that is supposed to cause it).

## C. The three-owner contract (the Phase 6 lesson, made structural)

```
PRE-GAME FORECAST      LIVE EVENT STATE      FINAL RESULT
immutable              ephemeral             canonical
model-owned            provider-owned        settlement-owned
```

- **Rule A** is enforced by absence: `forecast-join.mjs` exports six read-only functions and nothing
  that writes, so there is no call a future author could make. A test enumerates that surface.
- **Rule B**: freshness is derived on the READER's clock. One envelope is FRESH at +10 s, DELAYED at
  +60 s, STALE at +300 s — pinned with injected clocks, never `Date.now()` in a test.
- A live FINAL **does not settle anything**. No file write and no settlement call on the live path.

## D. Provider truth (verified 2026-09-15, both free, both already PUBLIC_DISPLAY in the registry)

| | MLB StatsAPI | ESPN public |
|---|---|---|
| verified | 200 · 86 KB · 15 games, **10 live** | scoreboard 200 · 158 KB · 16 events; summary 200 · **567 KB** |
| batch | ONE call serves the whole slate, in both modes | one call per slate date; `summary` per event, opt-in |
| identity | `gamePk` **is** our event id | event id **is** `/nfl/game/[eventId]`; athlete id **is** `nfl-athlete-<id>` |

⚠ **MLB ships no live player stats, and the blocker is the FORECAST side, not the feed.** MLB has no
published per-player range: the full-game simulation emits no per-player output even on a `ready`
game, and `mlb/player-props/<date>.json` is a bookmaker price list (American odds, provider names,
player by NAME, `team: null`, hashed gameId). A live MLB player stat would sit beside nothing or
beside a market price dressed as a GameTime projection. Pinned by a test that fails if per-player
simulation output ever appears.

⚠ **Neither provider publishes a source timestamp**, so `sourceUpdatedAt` is `null` and freshness is
stated from fetch age — the weaker claim, made as the weaker claim.

⚠ **ESPN is a public key-free surface, not a contracted API.** Technical access ≠ commercial licence.
This is founder decision 1 in §G.

## E. Found while verifying — each is now pinned by a test

1. ⚠ **ESPN's scoreboard defaults to the CURRENT week, and `dates=` is the event's ET date, not its
   UTC date.** DET @ BUF kicks off `2026-09-18T00:15Z` and is returned by `dates=20260917`; `20260918`
   returns **zero** events. Three Week 3 games rendered "no live data source covers this game" with
   their data one parameter away. The ET date is derived with `en-CA` in `America/New_York` so no
   manual part assembly can transpose month and day.
2. ⚠ **A browser can serve a stale refusal from its PRIVATE cache**, and a stale refusal is
   indistinguishable from a real one. `Cache-Control` now carries `max-age=0` alongside `s-maxage`, so
   only the shared edge may serve from cache. `max-age=0` is asserted explicitly, not assumed.
3. ⚠ **A family's model state varies per board** — `player_rush_yds` is PUBLISHED on the week-3 board
   and ESTIMATE on the week-2 one. The gate reads the state from the board in hand; hard-coding either
   answer would let a demoted family render on the week it was demoted.
4. ⚠ **My own banned-phrase guard fired on the comments documenting the ban.** The scanner now
   partitions code from string literals (a regex over raw text finds the words inside the very
   comments that forbid them; naive comment-stripping eats the `//` in a URL), and a mutation probe
   proves it still catches a real violation — including the inverse probe, that a comment about the
   ban is not itself a finding.

## F. Safety contracts, and how each is held

| contract | mechanism |
|---|---|
| no fake live probabilities | not expressible — the components take a value and a band and say where it sits. Banned-phrase scan over rendered strings, mutation-probed |
| no model mutation after kickoff | join module has no writer; frozen board proven byte-identical after two joins |
| no PAUSED/HOLDING promotion | only `PUBLISHED` families produce a row; demote every family and the comparison vanishes |
| ⚠ MLB totals stay PAUSED | `projectMlbForecast` exposes no combined total; a test asserts `totalRuns` never reaches a live surface |
| fail-closed identity | ambiguity → `AMBIGUOUS_EVENT_MAPPING`; unjoined rows counted, never name-matched; a summary naming another event is discarded |
| absent ≠ zero | postponed games expose no score; a blank cell is absent; a genuine feed `0` IS reported |
| no client secrets | no key, credential or `Authorization` anywhere on the live path (asserted) |

## G. Open founder decisions — public rollout is blocked on these

1. **ESPN usage posture for a public, continuously polled live surface.** The repository already uses
   this source class for schedules, results, rosters and injuries, but a public live surface on an
   uncontracted endpoint is a business call, not an engineering one. **MLB StatsAPI carries no such
   question** — if the founder prefers the narrow path, Stage 2 is MLB only and is ready.
2. **Where Live appears.** No navigation entry was added and none should be until (1) is answered.
3. **Re-measure cost under real traffic** before enabling NFL per-event player stats publicly.

Estimated upstream: **~1,000–9,000 requests/day**, all free endpoints, sports data **$0**. Vercel
footprint is expected to stay well inside the current plan and not approach the +$20/month threshold —
but an exact billing figure is deliberately NOT asserted, because it cannot be derived from the
repository. `FABLE_NEEDED`: **none** — no statistical question was reached.

## H. The five highest-value next tasks

1. **Set the two env vars (§A3)** and watch the first public slate day: `x-vercel-cache` HIT ratio on
   `/api/live`, function invocations, and whether polling really stops at FINAL under real readers.
   Write the observed numbers into `docs/GAMETIME_LIVE.md` §6 beside the measured ones.
2. **Then assess the next smallest step** — the founder named two candidates: a `/live` hub on the
   already-built batch `scoreboard` mode (one call per sport per slate, instead of N per-event), or
   resolving the NFL provider posture. The hub is a surface on existing, tested architecture; the NFL
   question is a business decision with the engineering already done.
3. **Mount on `/nfl/game/[eventId]`** if and when the NFL posture is approved — that page already owns
   the frozen forecast and the player board, so it is props wiring, not new architecture.
4. **Revisit the MLB player slice** only if `full-game-simulations` ever emits per-player output; a
   test fails loudly if it does. Until then there is no GameTime range to compare against.
5. **Carry the Phase 6 P2 debt forward** (untouched): `/mlb` payload, the five dead home components
   and the guards that assert on their unrendered copy.

## I. Unchanged from Phase 6

Model table unchanged — nothing was promoted, demoted or unpaused. NFL game forecasts EXPERIMENTAL ·
NFL receptions/receiving/rushing PUBLISHED · NFL passing yards ESTIMATE · NFL TD HOLDING · MLB winner
WATCH · **MLB totals PAUSED** · MLB run line HOLDING · EPL VALIDATED (live TOO_EARLY) · UFC VALIDATED
(live TOO_EARLY) · P318 STOP · P317 and P305-F accumulating. Phase 6 research debt (§J of the previous
handoff) and founder decisions (§L) all still stand.
