# GameTime Picks — Engineering Handoff (2026-09-15, Phase 5O, ~22:45 UTC)

Supersedes the Phase 5F version. For the next session at `/Users/yashwantbalaji/Downloads/gametimepicks`: read §A, §J (the
queue) and the memory index (`~/.claude/projects/-Users-yashwantbalaji/memory/MEMORY.md`, especially
`gtp-phase5o-launch-polish`, `gtp-phase5f-advanced`, `gtp-program-317-mlb-engine-level`). Charter unchanged: no paid data,
no wagers, no social posts, no protected-record edits, never promote a REJECTED/STOP model, never `git add -A`, merge never
rebase, batch `app/` pushes, research outside `app/`, never commit `vp/**` / `HANDOFF-2026-08-20.md` /
`docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / the pasted master prompts.

## A. Production state

| item | value |
|---|---|
| pushed this phase | `fbf1c17a6` — the single `app/` batch (Menu-sheet fix, /mlb packing, homepage trims, PA doc, 3 new tests) |
| CI | quality-gate run `35032429249` on `fbf1c17a6` — **completed success** (23:06:18Z; jobs `quality` + `python` both success) |
| production | serves **`fbf1c17a6`** exactly (`data/build-info.json` `commit.shortSha`, built 22:45:32Z) — the pushed commit itself, no ancestry inference needed |
| last full gate (final tree) | lint 0 · unit 6,279 tests (the one failure was a noisy guard I wrote; fixed, 12/12 on the three new files) · tsc 0 · build 0 · post-build 532 tests / 529 pass / **0 fail** · route inventory **73 routes, 0 findings** |
| founder-owned untracked | `HANDOFF-2026-08-20.md`, `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, `vp/**`, the pasted master prompts — leave them |

## B. What changed this phase (all public, one batch)

1. **🔴 The phone Menu sheet was broken and is fixed.** `mobile-bottom-nav.tsx` rendered `<MenuSheet>` inside the `<nav>`
   that paints `backdrop-filter: blur(14px)`. A filtered element is the **containing block for every `position:fixed`
   descendant**, so the sheet's `inset-0` resolved to the bar itself — measured **375×56** with 999px of content — and all
   18 destinations the bar does not carry (Results, the four sport hubs, the paper products, the record pages, Saved
   Forecasts) were squeezed into a 56px strip scrolling behind the bar. The dialog now renders outside the filtered
   subtree: 633px, Saved visible without scrolling at 375px. Pinned by `src/lib/uiux/mobile-menu-sheet.test.mjs`.
   The other two fixed dialogs (`site-search`, `mr-dub/ledger-calendar`) are safe — their blur is on the overlay itself.
2. **`/mlb` 1,771 KB → 1,511 KB** (−260 KB, −14.7%; decoded RSC 1,303 → 1,071 KB), identical rendered content:
   `recentGames` → `[date, opponent, isHome, value]` tuples decoded at the card boundary; an official StatsAPI headshot
   travels as `mlbPersonId` and is rebuilt to the identical URL (`mlbPersonIdFromHeadshotUrl`, round-trip checked — a
   non-canonical URL is kept verbatim, a missing portrait stays missing); the risk-ladder bench `pool` packs the same way
   through ONE `toSwapCandidate` (`lib/mlb/mlb-props.ts`) shared by `/mlb` and `/build`; the MLB report page maps through
   the canonical `toExplorerProjection` (O502-C). MLB report page 795 → 768 KB; `/build` 413 → 374 KB.
3. **Homepage busiest state 1,811 → 1,727 words** of the 1,820 ceiling (live 1,658 → 1,576) — 93 words of headroom,
   past the ≤1,750 target. Non-prediction copy only.
4. **Saved Forecasts** moved into the canonical NOW group; the sheet row carries the browser-local count.
5. **`PA_PER_GAME` doc** now states the realised ≈4.27 PA per lineup slot against the 3.85 divisor. Constant unchanged.

Owners touched: `lib/ui/explorer-projection.ts`, `lib/player-headshots.ts`, `lib/mlb/mlb-props.ts`, `lib/parlays/leg-swap.ts`,
`lib/navigation.ts`, `components/mobile-bottom-nav.tsx`, `components/ui/player-prop-{card,group}.tsx`,
`components/home/*`, `components/top-reads-panel.tsx`, `components/game/game-detail-page.tsx`, `app/{,mlb/,build/}page.tsx`.

## C. Performance before / after

| surface | before | after |
|---|---|---|
| homepage synthetic busiest state | 1,811 words | **1,727** (ceiling 1,820, unchanged) |
| homepage live | 1,658 words | 1,576 |
| `/mlb` total | 1,771 KB | **1,511 KB** |
| `/mlb` client/RSC payload (decoded) | 1,303 KB | 1,071 KB |
| MLB report page (atl-vs-chc) | 795 KB (prod) | 768 KB |
| `/build` | 413 KB (prod) | 374 KB |

Largest remaining `/mlb` contributors: the legacy SportShell `tabs` content ≈495 KB, the explorer `props` array ≈356 KB,
the bench `pool` ≈82 KB. (Report/`build` "before" values are production's, so a few KB is bot data drift, not the change.)

**O503 — measured, deliberately NOT refactored.** The Phase 5F theory that client wrappers serialise their server children
a second time is **wrong**: `DeferUntilVisible` renders only a placeholder into HTML, so its children exist once, in the
RSC payload. Next serialises the whole server tree regardless of client boundaries, so hoisting a heavy section out of the
wrapper would ADD it to the HTML — heavier, not lighter. No wrapper refactor was forced to hit a number.

## D. Operational receipts verified this session

- **NFL publication labels — DONE.** The 21:00Z `nfl-event-window` had not fired by 22:17Z, so it was dispatched once with
  `-f skip_odds=true` (zero credits; the odds step shows `skipped`). Run `35030214113`, artifacts at `22:18:24Z`:
  `model-status.json` carries `window {seasonType 2, week 2}`; passing yards **ESTIMATE** (`ESTIMATE_BELOW_BAR`), rushing
  yards / receiving yards / receptions **PUBLISHED** by `nfl-player-share-level-v1`; `index.json.generatedAt` ==
  `product-eligibility.json.indexGeneratedAt` == `2026-09-15T22:18:24Z`.
- **Week 2 forward file — immutable.** `replay/player-props-share-level-forward/2026-02.json`: exactly **one** commit
  (`780b35c4c`, 14:53Z). Receipt reads BLIND_FORWARD, all families ACCUMULATING (n=0).
- **P317 receipt — not yet due.** Only `engine-level-shadow/2026-09-15.json` (15 games) exists; the first `receipt.json`
  comes with the nightly settle (~05:30Z 09-16). Nothing here ever decides public MLB totals eligibility.
- **P305-F — not yet due.** First private rows at the Thursday (09-17) 21:00Z EPL matchweek run.

## E. QA evidence (O506)

Viewports 375px and desktop (1024), against the built export served by `gtp-static`:
`/`, `/today`, `/saved`, `/models`, `/markets`, `/simulate`, `/mlb`, MLB report page, `/nfl`, two NFL game pages,
`/results/nfl`, `/epl`, an EPL match page, `/ufc`, a UFC bout page. **No console errors anywhere. No horizontal overflow
anywhere** (`/today`'s wide table sits inside its intended `overflow-x: auto` container).
Internal-link audit: **532 links across 11 hubs, 0 missing targets.**
Card parity after the packing change, verified live on both surfaces: portrait rebuilt from person id loads, and the
drawer renders the same cells in the same order (`3 @LAD 09-09 · 0 @MIL 09-11 · 3 @MIL 09-12 · 4 @MIL 09-13 · 3 vsLAD 09-14`).

## F. Model / research state — unchanged, nothing reopened

NFL game forecasts EXPERIMENTAL · NFL player ranges FORWARD_TEST (passing yards **ESTIMATE**) · NFL TD HOLDING ·
MLB winner WATCH · MLB totals **PAUSED** · MLB run line HOLDING · EPL VALIDATED / live TOO_EARLY · UFC VALIDATED / live
TOO_EARLY. P318 **STOP**, P331 **BLOCKED ON DATA**, P317 and P305-F accumulating. No model research was done this phase.

## G. Known defects / debts

| # | item | priority |
|---|---|---|
| 1 | `/mlb` still 1,511 KB; the legacy SportShell tab content (~495 KB) is the next real lever, and it is a product question (does the legacy board still earn its place?) rather than a serialization trick | P2 |
| 2 | Homepage headroom is 93 words; a fifth first-class lane would eat it | P2 |
| 3 | The explorer `props` array (~356 KB) is 651 rows; a "top N + load more" contract would cut it, but that changes behaviour | P2 |
| 4 | `docs/ADOPTION_DASHBOARD_CONTRACT.md` verified complete (31 declared events, all documented, no duplicates, nothing undeclared emitted) — no action | — |

## H. Founder decisions (unchanged, carried forward)

1. **MLB finals backfill (yes/no):** may the pipeline backfill 2023–2025 regular-season final scores + venue from the free
   MLB StatsAPI schedule endpoint it already uses (~540 daily calls, ~4 MB, under `data/internal/mlb/linescores/`)? No
   approval means MLB totals differentiation stays blocked.
2. **UFC historical data source** — still unresolved.
3. **P317 adoption** — nothing to decide until its receipt reads `ENGINE_LEVEL_PASS` at n ≥ 200, and even then engine-level
   adoption does not unpause public MLB over/under.

## I. Launch status

**RELEASE CANDIDATE — no P0, no P1 open.** The one P1 found this session (the phone Menu sheet) is fixed, verified at
375px and pinned by a test. Remaining debt in §G is P2. The named research dependency (MLB totals differentiation) is
independent of launch.

## J. Next-session queue

**Opus / mechanical**
1. Decide the legacy `SportShell` question on `/mlb` (keep, trim to the tabs nobody duplicates, or retire) — the single
   biggest remaining payload item, ~495 KB, and a product call rather than a refactor.
2. Sweep for other `position: fixed` children under filtered/transformed ancestors if any new overlay ships (the class
   that broke the Menu sheet); the two existing dialogs are already clean.
3. Optional: explorer "top N + load more" if `/mlb` must go below ~1.3 MB.

**Fable / research (do NOT attempt in Opus)**
- MLB totals differentiation, once the founder answers §H1.
- P317 engine-level adoption judgment at its registered threshold; P305-F adoption; any new P318 protocol.
