# GameTime Picks — Engineering Handoff (2026-09-15, Phase 2, ~07:40 UTC)

Supersedes the 05:00 UTC version of this file. For a new Claude Code session at `/Users/yashwantbalaji/Downloads/gametimepicks`.
Read §A first, then the memory index (`~/.claude/projects/-Users-yashwantbalaji/memory/MEMORY.md`, especially
`gtp-command-center-contract`, `gtp-simulation-story`, `gtp-phase2-saved-brief-analytics`, `gtp-program-305-soccer-totals-replay`).
The standing charter is unchanged (no paid data, no protected-record edits, no wagers, no social posts, never promote a
REJECTED model, never `git add -A`, never commit `vp/**` / `HANDOFF-2026-08-20.md` / `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`,
merge never rebase, batch `app/` pushes). There is no `AGENTS.md` in the repo.

## A. Production state

| item | value |
|---|---|
| pushed public SHAs (this phase, in order) | `2c50ffa10` P308 · `9f31288ab` P310 (merge) · `ea07c7ac2` P311 (merge) · `c0db4365c` analytics · then research/handoff commits (root only, no site build) |
| verified live in the browser | `c0db4365` live (built 07:10Z): `/today` brief, `/saved` empty state, the MLB report story checked on prod with no console errors (use the in-app browser for `data/build-info.json`; curl gets a Vercel security checkpoint) |
| CI | quality-gate **green** on `9f31288ab` (`34934848968`), `ea07c7ac2` (`34936221651`) and `c0db4365c` (`34940303867`) — every public commit of this phase |
| founder-owned untracked | `HANDOFF-2026-08-20.md`, `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, `vp/**` — leave them |

## B. What changed for users this phase (all pushed)

1. **Simulation Story** (P308) on the MLB report (under the hero), the NFL game page, the EPL match page and the UFC hub:
   chapters from the existing presentation adapters, played inline with Play/Pause/step/Replay and a skip-to-report
   anchor; bars only reveal artifact values; every chart has a screen-reader sentence; reduced motion removes the clock and
   the growth; the reveal says "Reading the outcome distribution", never "running". A refusal prints its reason.
2. **Run-count honesty** (P308 stage 0): a missing artifact run count is null and forbids every "N runs" phrase (the NFL
   builder used to default to 10,000); the NFL hub, NFL game metadata and the MLB report read the artifact's own count.
3. **Saved Forecasts** (P310): Save on the shared card (homepage lanes) → `/saved` (noindex; rail + footer) with
   Upcoming / Started · pending / Final, results joined from the canonical ledgers only, immutable snapshots, clear-all.
4. **GameTime Brief** (P311) on `/today`: what changed (model-state transitions from the scorecard's own record; MLB
   forecast moves between each game's first and last pre-kickoff snapshot) plus the reader's own layer (saved, followed).
5. **Pauses are scoped to wired families**: a BREACHED family the gate is not wired for reads Watch, never Paused.
6. **Analytics vocabulary**: 7 new events declared, validated, inventoried and emitted through the no-op sink (provider off).

## C. Canonical owners (new or changed)

- Story: `app/src/components/simulate/simulation-story.tsx` (+ `simulation-story-section.tsx`), pure rules in
  `app/src/lib/simulate/presentation/story-controls.mjs`; adapters unchanged (`lib/simulate/presentation/*`).
- Saved: `app/src/lib/saved/{saved-schema.mjs,saved-store.ts,results.mjs}`; card contract field `settlement`.
- Brief: `app/src/lib/command-center/changes.ts`; scorecard transitions `app/src/lib/ops/health-changes.mjs` (the builder
  writes `changes` on `admin/model-health.json`).
- Analytics: `app/src/lib/analytics/event-contract.ts` + `api/_collect-core.mjs` + `docs/ADOPTION_DASHBOARD_CONTRACT.md` §6.
- Budgets: `/simulate` 600 KB (measured 268).

## D. Research / model work

| program | state | evidence | verdict / public |
|---|---|---|---|
| P305 EPL totals | REJECTED (blind, 1X2 ECE ceiling) · SECOND_LOOK_ELIGIBLE (EPL) | `a6bacbe57` | not adopted; founder said WAIT for the shadow |
| P305-F shadow | registered + wired | first rows Thu 2026-09-17 21:00Z (`epl-matchweek`) | research only; /ops + /models say so |
| MLB totals diagnosis | DONE (diagnosis only) | `data/internal/research/mlb/reports/totals-overconfidence-diagnosis.json` | **LEVEL defect**: sim mean 7.80 vs line 8.25 vs actual 9.36 on 253 joined games; bias grows with the line (+0.5 / +1.8 / +3.2); dispersion secondary (PIT top decile 0.20, coverage 0.76); selection downstream. Pause stays. Next: dev sensitivity of the engine's league priors (`lib/mlb/full-game/plate-appearance.ts`), then a forward-only registration |
| NFL passing yards | DESIGN NOTE only | `data/internal/research/nfl/reports/passing-yards-candidate-design.md` | ESTIMATE stays; 2014–21 is a second look for this family |
| P304 EPL forward | ACCUMULATING 0/60 | `data/internal/research/epl/forward/receipt.json` | live |
| NFL share-level forward | ACCUMULATING, no week yet | receipt in `data/internal/research/nfl/replay/player-props-share-level-forward/` | live (receipt-gated) |

## E. Model status table — unchanged from the morning (scorecard 2026-09-14T16:01Z)

NFL game forecasts EXPERIMENTAL · NFL player ranges FORWARD_TEST (pass yds ESTIMATE) · NFL TD HOLDING (168) · MLB winner WATCH (605) ·
MLB totals **PAUSED** (576) · MLB run line HOLDING (605) · EPL match model VALIDATED, forward 0/60, live TOO_EARLY (35) ·
EPL club totals SHADOW · Ligue 1 ACCEPTED_V1, live n 9 · UFC fight model VALIDATED, live TOO_EARLY (31).

## F. Test evidence (last full gate, analytics commit)

```
lint:scripts                       exit 0
run-suite --phase unit             # pass 6230  # fail 0
tsc --noEmit                       exit 0
build                              exit 0   (/ 218 KB · /today 582 · /saved 71 · /simulate 268 · report ~738)
run-suite --phase post-build       # pass 529  # fail 0
build-route-inventory              73 routes · findings 0 (P0 0)
```
Browser: story plays on the MLB report (desktop + 375px, no overflow, no console errors); save → `/saved` round trip with all
four ledgers fetched 200; `/today` brief renders "quiet is a real answer" on a quiet night. Reduced motion verified by the
pure rules' tests (no auto-advance, zero growth), not by an emulated media query.

## G. Known defects / debts (real)

1. **MLB totals repair not registered** — the diagnosis names the class (level); a candidate needs a dev sensitivity study
   of the engine priors first. Severity: high for the product (the most visible paused family). Next: the study, then a
   forward-only registration per the plan.
2. **NFL passing-yards candidate not registered** (design note only). Severity: medium.
3. **Report pages carry no Save control** — saving exists on the homepage cards only; the report pages would need a
   `cardFor(event)` builder. Severity: medium (retention).
4. **Story on a settled NFL game refuses** ("no published simulation for this date") because settled games leave the
   eligibility list; an archived pre-kickoff story would need the frozen forecast fed to the adapter. Severity: low.
5. **`/saved` not in the mobile bottom bar** (rail + footer + menu only; the bar has a character budget). Severity: low.
6. **Story controls are 32px tall** at phone width; 36–44 would be kinder. Severity: low.
7. **UFC story is card-level** on the hub, not per bout page. Severity: low.
8. **No service worker** (deliberate: no stale-forecast risk); manifest is installable. Not a defect.

## H. Time-sensitive follow-ups (none of these had happened at handoff)

- **Tue 13:30Z** `nfl-props-forward-shadow` → `data/internal/research/nfl/replay/player-props-share-level-forward/2026-02.json`
  (check `p25/p75/espnId`, `anytime_td` rows, `state.earlierGamesWithoutStatRows`, commit time < 2026-09-18T00:15Z);
  **14:30Z** `nfl-event-window` → Week 2 boards with share-level families, Week 1 reconciliation to 16 final.
- **Thu 21:00Z** `epl-matchweek` → private rows carry `shadowTotals`, public rows do not, `forward-totals/receipt.json`
  appears ACCUMULATING.
- Nightly scorecard: with `changes` now recorded, the first transition will show on `/today`'s brief and `/models`.

## I. Founder decisions still required

1. UFC historical data source (unchanged).
2. Whether the MLB totals repair may start as a registered candidate once the sensitivity study is in (no policy change
   needed to run the study).

## J. Next 5 highest-value tasks

1. Verify §H windows the moment they pass (first live evidence for P300/P301 and P305-F).
2. MLB totals: engine-prior sensitivity study on dev (July–Aug) → register a level candidate forward-only.
3. Save control on report pages + an archived story for started games.
4. NFL passing-yards candidate: dev exploration per the design note; register only if it clears dev bars.
5. Busy-day homepage word re-measure (ceiling 1,820; quiet night 1,540) and P313 touch-target pass on the new controls.
