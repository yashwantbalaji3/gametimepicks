# GameTime Picks — Engineering Handoff (2026-09-15, ~05:00 UTC)

For a new Claude Code session at `/Users/yashwantbalaji/Downloads/gametimepicks`. Supersedes the 2026-09-15 02:00 UTC scratchpad
handoff; everything from that one that still matters is folded in. Read §0 and §A before touching anything. The standing
charter (no purchased data, no protected-record edits, no wagers, no social posts, no promotion of rejected models, never
`git add -A` at root, never commit `vp/**` or `HANDOFF-2026-08-20.md` or `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`,
never rebase, never print secrets, batch `app/` pushes) is unchanged. There is no `AGENTS.md` in the repo (searched); the
rules live in memory (`~/.claude/projects/-Users-yashwantbalaji/memory/`) and in this file.

## A. Production state

| item | value |
|---|---|
| final local SHA | see `git log -1` (this file's commit is the last of the session) |
| final pushed public SHA | `b6cbf2e95` (Model Lab) — preceded by `0cadb5cff` (hub panels), `94b9de168` (Command Center), `0eb13ddee`/`a385f729f` (P305-F), `ba2ed5bce` (market-layer pause) |
| deployment | `b6cbf2e9` verified live at 04:44Z via the browser, `/models/` renders on prod (curl gets a Vercel security checkpoint now — verify deploys in the in-app Browser, poll `data/build-info.json` `commit.shortSha`, never faster than ~60s) |
| CI | quality-gate on `b6cbf2e95` (run 34929913416) **green** (quality + python), covering every commit of this session; the runs on `94b9de168` and `0cadb5cff` were superseded by the next push |
| dirty / untracked founder files | `HANDOFF-2026-08-20.md`, `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, `vp/**` — leave them |
| local scratch | none in the tree; a static preview server (`gtp-static`, port 4173) may still be running in the desktop app |

## B. What changed for users (all pushed; §A says what was verified live)

1. **Homepage Command Center** (`/`): the "Simulation Hub" and "Other coverage" sections are now lanes of one product for
   NFL, MLB, Premier League and UFC — the owner's context line, an "Updated … ET" stamp from the artifact, two model-status
   chips (public vocabulary, evidence in the tooltip), and the next forecast through the universal card or a designed
   empty line. Ligue 1 appears inside the soccer lane when it has forecasts. 1,540 rendered words against the 1,820 ceiling.
2. **Model status on the hubs** (`/mlb`, `/epl`, `/ufc`): a full panel per hub — every family, its state, the evidence
   sentence, "What does this mean?". NFL keeps its receipt-driven coverage table.
3. **Model Lab** (`/models`, in the rail and footer): live models, experiments and shadows, recent decisions, why a model
   pauses, the words we use. Everything derived from receipts at build time.
4. **Status glossary** on `/methodology#model-status` — the same map the chips link to.
5. **MLB pauses reach the model-vs-market blocks** on `/markets` and the game report (`ba2ed5bce`): a paused family says
   "Model side paused" instead of quoting a probability the hero withdrew. Moneyline and run line can now be paused the
   way the total is (no visible change until a family breaches; moneyline is WATCH).
6. **NFL hub** no longer links a week page that does not exist yet (the Monday-after-Week-1 dead link).

## C. Product architecture changes (canonical owners)

- **Forecast contract:** `app/src/lib/command-center/contract.ts` — `PredictionCardModel`, `ConfidenceSignal` (only MLB has
  a tier; every other sport is a probability), `PublicModelState` + `PUBLIC_STATE_LABEL/MEANING`.
- **Model status / freshness:** `app/src/lib/command-center/model-status.ts` (health scorecard → PAUSED only where the
  live-record gate would act; stale scorecard ⇒ never PAUSED), `freshness.ts` (artifact stamp vs build instant; owner
  SOURCE_STALE wins). Components: `app/src/components/command-center/{model-status-chip,model-status-panel,prediction-card,command-center}.tsx`.
- **Homepage lanes:** `app/src/lib/command-center/command-center.ts` — fed by the product-day owner (`buildProductDays`);
  hub membership is still `partitionSports`; featured cards from `featured.ts` (MLB rows through `pauseMlbMarkets`; EPL
  features only CURRENT_PRE_EVENT rows = what the owner counts).
- **Model Lab:** `app/src/lib/command-center/model-lab.ts`.
- **Simulator:** unchanged (P308 not started). Owner map is in memory `gtp-command-center-contract` and the simulator
  audit summary below.
- **Saved picks / personalization:** not built (P310/P315). Reuse `lib/follow/follow-store.ts` (localStorage, key
  `gtp.follow.v1`, cross-tab event) as the pattern; accounts (P266) stay off until Supabase keys.

## D. Research / model work

| program | registration | receipt | verdict | forward state | public |
|---|---|---|---|---|---|
| P305 EPL club-specific totals | `2608ff2bb` | `a6bacbe57` | BLIND **REJECTED** (1X2 ECE 0.032 > 0.03; control 0.0326) · EPL **SECOND_LOOK_ELIGIBLE** (both candidates) | — | not adopted; founder decision (§I) |
| P305-F totals shadow | `a385f729f` (protocol, before any shadow forecast) | `data/internal/research/epl/forward-totals/receipt.json` (written by the matchweek run) | — | first rows on the next `epl-matchweek` run (Thu 21:00Z); minN 60 | never public; /ops + /models say research only |
| P304 EPL Elo-Poisson | `2b8921861` | `e8346033e` ELIGIBLE | live | forward receipt ACCUMULATING n 0/60 | live |
| P300/P301 NFL player families | see memory | second-look / TD ELIGIBLE | live (receipt-gated) | forward receipt ACCUMULATING (no week yet) | live; pass-yds ESTIMATE |
| MLB totals | — | health `mlb_total` BREACHED (n 576) | — | — | **paused** by the live-record gate; diagnosis plan at `data/internal/research/mlb/reports/totals-overconfidence-diagnosis-plan.md` (plan only) |

## E. Current model status (as the build read it at handoff)

| sport | family | state |
|---|---|---|
| NFL | game forecasts | EXPERIMENTAL (win/margin/totals heads live, tested 2000–2021) |
| NFL | player ranges | FORWARD_TEST (Week 2 file pending nflverse MNF stats) · passing yards ESTIMATE |
| NFL | touchdown chances | HOLDING (n 168) |
| MLB | winner calls | WATCH (n 605) |
| MLB | game totals | **PAUSED** (n 576) |
| MLB | run line | HOLDING (n 605) |
| EPL | match model | VALIDATED (blind 2013–22) · forward test 0/60 · live record TOO_EARLY (n 35) |
| EPL | club totals | SHADOW (research only) |
| Ligue 1 | match model | ACCEPTED_V1 on its own study; live record too small (n 9); no Command Center lane of its own (secondary under soccer) |
| UFC | fight model | VALIDATED (backtest) · live record TOO_EARLY (n 31) |

## F. Test evidence (last full gate, on the Model Lab commit)

```
npm run -s lint:scripts                       → exit 0
node scripts/ci/run-suite.mjs --phase unit    → # pass 6217  # fail 2 → both fixed (route table), audit + nav tests re-run green (60/0)
npx tsc --noEmit                              → exit 0
npm run -s build                              → exit 0  (/ 224 KB, /models 158 KB, /nfl 447, /epl 425, /ufc 601, /mlb 1510 — all inside budgets)
node scripts/ci/run-suite.mjs --phase post-build → # pass 523  # fail 0
build-route-inventory.mjs                     → 72 routes · findings 0 (P0 0)
```
Earlier full runs this session: unit 6215/0 (hub panels), 6212→fixed (Command Center). Browser checks: `/` at 375px stacks
with no overflow; `/mlb` panel states WATCH/PAUSED/HOLDING; `/models` renders with no console errors.

## G. Known defects / debts (real, unresolved)

1. **P308 Simulation Lab 2.0 not started.** The animated presentation player is dormant by founder decision (P242) and
   `e2e/p242-direct-report.spec.ts` asserts no dialog on report pages — reviving it is a founder-visible reversal. One
   known honesty leak to fix first: `src/lib/game-detail.ts:678` defaults NFL `runCount` to 10000, which
   `game-lab-view.ts:147` turns into permission to print a run-count claim. Severity: medium.
2. **Analytics events for the new surfaces** (`sport_selected`, `model_status_opened`, `model_lab_opened`…) are not in
   `lib/analytics/event-contract.ts`; provider is OFF anyway. Severity: low.
3. **NFL "1 games forecast today"** grammar comes from `nfl/model-status.json` (upstream builder), now visible on /models.
   Severity: cosmetic.
4. **Ligue 1 has no product-day owner** (`ProductDay["sport"]` lacks it), so it cannot be a first-class lane. Severity: low.
5. **Simulator adapters carry no paused-market handling for NFL/EPL/UFC** (gate is MLB-only today). Severity: none until a
   gate exists for those sports.
6. **`first-viewport-budget` word ceiling (1,820)** leaves ~280 words on a quiet night; a busy day (two ranked panels + a
   full slate) measured 1,739 before this session — re-measure the first busy day after the Command Center. If it trips,
   trim "How it works" (283 words) rather than raising the ceiling.

## H. Time-sensitive follow-ups

- **Tue 2026-09-15 13:30Z** `nfl-props-forward-shadow`: expects the Week 2 blind forecast `2026-02.json` once nflverse has
  DEN@KC rows (KC 21–10 DEN in the 3rd at 02:20Z). Verify columns `p25/p75/espnId`, `anytime_td` rows, commit time <
  2026-09-18T00:15Z. Then the 14:30Z `nfl-event-window` should publish share-level families on Week 2 boards and settle
  Week 1 to 16 final on `/results/nfl`.
- **Thu 2026-09-17 21:00Z** `epl-matchweek`: first run with the P305-F shadow — check the private forecast rows carry
  `shadowTotals`, the public artifact does not (test pins this), and `data/internal/research/epl/forward-totals/receipt.json`
  appears (ACCUMULATING).
- **Sat–Mon** EPL matchweek 5 grading: graded rows gain `control` and `shadowTotals`; forward receipts start moving.
- Nightly `model-health.json`: MLB total pause stays until it stops reading BREACHED; the homepage lane and /models follow it.

## I. Founder decisions required (actionable now)

1. **P305 adoption or not** — the blind receipt is REJECTED on a bar the control also fails; the shadow is running. Options:
   (a) wait for the shadow (recommended; SHADOW_BETTER needs 60 matches), (b) adopt on the second look with the defect
   stated in public copy (an exception, like the passing-yards ESTIMATE).
2. UFC historical data source (unchanged, still open).
3. NBA investment (unchanged, still open).
4. Whether to delete the local raw football-data.co.uk CSVs (unchanged).

## J. Next recommended programs (by user impact and model risk)

1. Verify the two time-sensitive windows in §H; they are the first live evidence for P300/P301 and P305-F.
2. P308 Simulation Lab 2.0, scoped honestly: fix the NFL run-count default first; then a live (non-dormant) presentation
   on the report pages that reads `PresentationChapter` output and honours `pausedReasons`.
3. MLB totals diagnosis (plan in `data/internal/research/mlb/reports/`), then a registration — the paused call is the most
   visible "why is this off?" on the site.
4. NFL passing-yards new candidate registration (a genuinely different family; P300's is REJECTED and stays so).
5. P310 saved forecasts on the `follow-store` pattern, with results joined from `public/data/<sport>/graded-picks.json`
   (confirm those files survive the `out/data` prune — deny-by-default).
6. Ligue 1: note that its history 2017-18→2021-22 was SCORED for 1X2 in P305 (the control), so a P304-style replay there is
   a second look, not blind. Disclose if registered.

## Simulator audit summary (for P308)

Routes `/simulate`, `/simulate/d/[date]`, `/games` alias; presentation adapters in `src/lib/simulate/presentation/*` for
MLB/NFL/EPL/UFC/boards (chapter kinds event/outcome/distribution/margin/scores/players/limits/closing); the animated
`presentation-player.tsx` is dormant (P242); the live animated surface is `simulation-stage.tsx` for non-ready states only.
Run counts are gated by `allowsRunCountClaim`; MLB totals bins are objects, EPL totals are a raw number[]; three artifact
families coexist (`lib/mlb/full-game`, `lib/full-game-sim`, `lib/game-simulations`). No page-weight budget on `/simulate`.
