# GameTime Picks — Engineering Handoff (2026-09-15, Phase 5F, ~21:45 UTC)

Supersedes the Phase 4 version. For the next (Opus) session at `/Users/yashwantbalaji/Downloads/gametimepicks`: read §A, §J (the
queue) and the memory index (`~/.claude/projects/-Users-yashwantbalaji/memory/MEMORY.md`, especially `gtp-phase5f-advanced`,
`gtp-phase4-launch-hardening`, `gtp-program-317-mlb-engine-level`, `gtp-phase3-save-archive-guards`). Charter unchanged: no paid
data, no protected-record edits, no wagers, no social posts, never promote a REJECTED/STOP model, never `git add -A`, never commit
`vp/**` / `HANDOFF-2026-08-20.md` / `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / the pasted master prompts, merge never rebase,
batch `app/` pushes, research outside `app/`.

## A. Production state

| item | value |
|---|---|
| pushed this phase | `0082d9953` P318 v2 design (pre-look) · `cd0e8ff38` P318 v2 STOP · `21c1e15a7` P331 prerequisites · `d7a46bba7` (merge of the app batch: explorer projection + P317 separation test) |
| CI | quality-gate on `d7a46bba7`: run `35026059106` — in_progress  |
| production | `d7a46bba` served exactly (built 21:32Z) — the pushed merge itself, no ancestry inference needed (read `data/build-info.json` `commit.shortSha` in the in-app browser; curl hits the Vercel checkpoint; bots deploy descendants — verify ancestry with `git merge-base --is-ancestor`) |
| last full gate (final tree) | lint 0 · unit 6,266 pass / 0 fail · tsc 0 · build 0 · post-build 0 fail (529 checks) · route inventory 73 routes, 0 findings |
| founder-owned untracked | `HANDOFF-2026-08-20.md`, `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, `vp/**`, the pasted master prompts — leave them |

## B. What changed this phase

1. **`/mlb` payload** (live): the player-props explorer (a client component) now receives a typed projection of the eighteen
   fields it renders instead of full lean rows. `/mlb` 1,893 KB → 1,770 KB. Owner: `app/src/lib/ui/explorer-projection.ts`;
   `lib/world-cup/player-model-picks.ts` is generic over the row type.
2. **P317 separation pinned** (test only): `app/src/lib/mlb/full-game/engine-level-separation.test.mjs` — only the candidates
   module and the full-game generator know the engine-level candidate; the live-record gate reads the scorecard only.
3. Research (root only): P318 v2 design + STOP; P331 prerequisites (founder data request + starter-capture contract).

## C. Research state

| program | state | evidence | public |
|---|---|---|---|
| P318 passing yards, decomposed v2 | **STOP** after the one authorised look (fit 2022–23, assessed 2024–25): ECE 0.0516 vs 0.05; coverage 0.791; level 1.004; MAE 61.1 vs 65.8 | `data/internal/research/nfl/reports/passing-yards-decomposed-v2-{design,stop}.md`, `-assessment.json` | ESTIMATE stays; only the 2026 forward record is new evidence |
| P331 MLB differentiation | **BLOCKED ON DATA** — one-line founder decision (backfill 2023–25 finals from the approved StatsAPI endpoint); starter run prevention = forward capture contract | `totals-differentiation-prerequisites.md`, `totals-differentiation-no-go.md` | over/under stays PAUSED |
| P317 engine-level shadow | FORWARD SHADOW accumulating; first receipt after the first nightly settle since wiring (~05:30Z 09-16) | `data/internal/research/mlb/engine-level-shadow/` | never call eligibility |
| P305-F EPL totals shadow | first rows Thu 2026-09-17 21:00Z | — | never |
| NFL share-level forward | Week 2 file one commit (`780b35c4c`), on time | boards publish `nfl-player-share-level-v1` for receptions / receiving / rushing | live |

## D. Cross-surface truth audit (Phase 5F §6)

1 PAUSED never leaks — PASS (`markets/pairing.test`, `ops/live-record-gate.test`, `command-center.test` "a paused family cannot reach
the card"). 2 labels from period boards — PASS in code (`family-publication` LIVE test); the committed artifacts show it after the next
event window. 3 saved settlement joins only its market — PASS (`saved-integrity.test`). 4 archived stories from the named frozen
revision — PASS (`archived-forecast.test`, receipts). 5 private fields out of public artifacts — PASS (`totals-shadow.test` publicRows,
status builder self-check, export prune deny-by-default, commit-scope pin; engine shadow lives under `data/internal`). 6 freshness from
the owner's stamp — PASS (`command-center.test` "never the browser"). 7 started events cannot be saved — PASS (`saveEligibility`,
client clock). 8 deploy verification by ancestry — PASS as process (handoff + memory; no bot SHA is ever asserted equal to the human SHA).
No gap found; the only guard added this phase is the P317 separation test.

## E. Model status table — unchanged (scorecard 2026-09-15T14:29Z, `changes: []`)

NFL game forecasts EXPERIMENTAL · NFL player ranges FORWARD_TEST (pass yds ESTIMATE) · NFL TD HOLDING · MLB winner WATCH · MLB totals
**PAUSED** · MLB run line HOLDING · EPL match model VALIDATED, live TOO_EARLY · UFC fight model VALIDATED, live TOO_EARLY.

## F. Time-sensitive windows (verify from artifacts; never wait)

- **Next `nfl-event-window`** (the 21:00Z schedule had not fired by 21:31Z): `model-status.json` gains `window` + PUBLISHED family
  rows; `index.json` follows; `product-eligibility.json.indexGeneratedAt` equals the index stamp (the `family-publication` LIVE test
  asserts the labels). If it did not regenerate, dispatch with `-f skip_odds=true` (zero credits; a PENDING dispatch can be cancelled
  by the shared concurrency group — re-dispatch once).
- **~05:30Z 09-16 nightly-settle**: `engine-level-shadow/receipt.json` with `graded > 0`, ACCUMULATING; morning production writes
  `engine-level-shadow/2026-09-16.json`.
- **Wed 13:30Z**: the Week 2 forward file stays one commit. **Thu 21:00Z**: P305-F private rows carry `shadowTotals`, public rows do not.

## G. Known defects / debts

1. Busiest-day homepage headroom 9 words (1,811 of 1,820). Medium → queue item 1.
2. `/mlb` 1,770 KB: 1,467 KB is client payload — recentGames 195 KB, headshot URLs 134 KB, ids 47 KB inside the explorer array; risk
   ladder `pool` 117 KB; client wrappers (`DeferUntilVisible`, `SportShell`, `sport-hub-nav`) serialise their server-rendered children a
   second time (≈ 400 KB of `children/className/style` keys). Medium → queue items 2–4.
3. P330 labels visible only after the next event window. Low.
4. `/saved` not in the mobile bottom bar. Low → queue item 6.
5. `PA_PER_GAME` doc defect in the engine (realised 4.27 PA per slot). Low.

## H. Founder decisions

1. **MLB finals backfill (yes/no):** may the pipeline backfill 2023–2025 regular-season final scores + venue from the free MLB StatsAPI
   schedule endpoint it already uses (~540 daily calls, ~4 MB, committed under `data/internal/mlb/linescores/`)? Yes unlocks a
   preregistered park-environment differentiation candidate; no keeps MLB totals research blocked.
2. UFC historical data source (unchanged).
3. P317 adoption only if its receipt reads ENGINE_LEVEL_PASS at n ≥ 200 (nothing to decide yet).

## I. Launch status

**READY WITH ONE NAMED RESEARCH DEPENDENCY** (MLB totals differentiation). No P0. P1: homepage headroom; `/mlb` weight. P2: the rest of §G.

## J. Opus 5 queue (ranked; do these without Fable)

1. **Homepage headroom** — trim 20–40 non-prediction words: `components/home/featured-simulations.tsx` run-count label per card,
   `components/home/home-sections.tsx` flagship blurbs, `lib/navigation.ts` footer descs. Accept: `busiest-state.test` reports ≥ 30 words
   under the ceiling; `first-viewport-budget.test` green. Build needed: yes. Batch: yes.
2. **`/mlb` recentGames as tuples** — serialise `recentGames` as `[date, opponent, isHome, value]` in `lib/ui/explorer-projection.ts`
   and decode in `components/ui/player-prop-card.tsx`. Accept: /mlb payload −60 KB or more; card renders identically (screenshot).
   Build: yes. Batch: yes.
3. **`/mlb` headshot by id** — pass the StatsAPI person id instead of the full `photo` URL and rebuild the URL in the card via the
   existing headshot helper (memory: free-statsapi headshots). Accept: −80 KB or more; portraits unchanged. Build: yes. Batch: yes.
4. **`/mlb` client wrappers** — measure which of `DeferUntilVisible` / `SportShell` / `sport-hub-nav` wrap the largest server subtrees
   (count `children` keys in the RSC payload per wrapper) and move heavy server sections outside the wrapper or pass a slot. Accept:
   RSC payload −200 KB or more, no behaviour change (deferred sections still defer). Build: yes. Batch: after 2–3 land.
5. **Explorer projection on the game-detail page** — `components/game/game-detail-page.tsx` line ~568 hands the explorer full rows;
   map through `toExplorerProjection`. Accept: MLB report pages lose ~10–15% payload; tests green. Build: yes. Batch: yes.
6. **`/saved` phone discoverability** — a compact entry in the mobile menu (`components/mobile-bottom-nav.tsx` budget audit first).
   Accept: reachable in two taps at 375px without a bar change. Build: yes. Batch: yes.
7. **Verify the 21:00Z labels + tonight's shadow receipt** (§F) and record them in this file. Build: no. Batch: n/a.
8. **Reword `PA_PER_GAME` doc** in `lib/mlb/full-game/plate-appearance.ts` to state the realised divisor honestly. Build: yes (app/)
   — batch with any of the above.
9. **Exhaustive 375/390/desktop sweep** of the four hubs and report pages after items 1–5. Build: no (use the built export).
10. **Docs**: fold `docs/ADOPTION_DASHBOARD_CONTRACT.md` event rows for Phase 3–4 into one table pass. Build: no.
