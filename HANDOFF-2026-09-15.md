# GameTime Picks — Engineering Handoff (2026-09-15, Phase 4, ~20:00 UTC)

Supersedes the Phase 3 (~16:00 UTC) version of this file. For a new Claude Code session at `/Users/yashwantbalaji/Downloads/gametimepicks`.
Read §A first, then the memory index (`~/.claude/projects/-Users-yashwantbalaji/memory/MEMORY.md`, especially
`gtp-phase4-launch-hardening`, `gtp-program-317-mlb-engine-level`, `gtp-phase3-save-archive-guards`, `gtp-command-center-contract`,
`gtp-simulation-story`, `gtp-phase2-saved-brief-analytics`). The standing charter is unchanged (no paid data, no protected-record
edits, no wagers, no social posts, never promote a REJECTED model, never `git add -A`, never commit `vp/**` / `HANDOFF-2026-08-20.md` /
`docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / the pasted master prompts, merge never rebase, batch `app/` pushes). No `AGENTS.md`.

## A. Production state

| item | value |
|---|---|
| pushed this phase (in order) | `dad484788` P331 no-go + P318 stop (research only, root) · `fcffaeee2` P329/P330/P323 (merge `ac319b008`) |
| CI | quality-gate on `ac319b008`: run `35014339100` — completed success |
| production | `3cccf6a6` (bot lineup-refresh descendant of `ac319b008`, built 19:46Z) verified: the trimmed homepage copy renders, no console errors (read `data/build-info.json` `commit.shortSha` in the in-app browser; curl hits the Vercel checkpoint; bots deploy descendants, so verify ancestry) |
| earlier this day | Phase 3 batches `1207df43d`, `d597f0208`, `8c9f1ffb1`, `0b4060197` all CI-green and served |
| founder-owned untracked | `HANDOFF-2026-08-20.md`, `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, `vp/**`, `GameTimePicks_Claude_Code_Master_Prompt_Phase_3_2026-09-15.md` — leave them |

## B. What changed for users this phase (all pushed)

1. **Busiest-day homepage** (P329): explanatory copy trimmed at its sources (~110 words: simulation-card frequency and
   market-count lines, How-it-works bodies and subtitle, 21 nav-rail descriptions, parlay explainer and heading, top-reads
   explainers, dated-ahead sub, recap sentence, disclaimer duplicate, footer heading, closed-lane reason). Live homepage
   1,658 words (was 1,766); the synthetic four-lane evening projects 1,811 against the 1,820 ceiling. Predictions untouched.
2. **NFL family labels from publication truth** (P330): the public status and index derive each player family from the
   boards of the window's own period — PUBLISHED with its model, ESTIMATE with the board's reason, withheld, or honestly
   mixed. The committed `model-status.json`/`index.json` were deliberately left to the 21:00Z event window (their stamps are
   coupled to `product-eligibility.json`); the labels show after that run.
3. **Following filter matches MLB clubs** (P323 fix): slate rows carry full club names (from each game's prediction) beside
   the codes, which is what the follow store keys.

## C. Canonical owners (new or changed)

- `app/src/lib/uiux/busiest-state.test.mjs` — the four-lane fixture rendered through the real `CommandCenter`; bars: fixture
  lanes ≤ 400 words (pure) and non-lane(built) + fixture + 69 ≤ 1,820 (built). Trim copy; never raise either.
- `app/src/lib/sports/nfl/family-publication.mjs` — `derivePlayerFamilyPublication`; wired in `build-nfl-public-status.mjs`
  (writes `window`); closed state set in `public-route-inventory.test` gained PUBLISHED / MIXED; the functional status tests copy
  this module beside the script.
- `app/src/lib/today/slate-games.ts` — `teamNames` on every row.
- Research: `scripts/research/mlb/venue-run-environment.mjs`, `scripts/research/nfl/explore-passing-yards-decomposed.mjs`.

## D. Research / model work

| program | state | evidence | verdict / public |
|---|---|---|---|
| P331 MLB totals differentiation | **NO-GO** (documented) | `data/internal/research/mlb/reports/totals-differentiation-no-go.md`, `venue-run-environment-dev.json` | a walk-forward one-season venue factor is noise (corr 0.006 with actuals; the line prices the venue, corr 0.38); no pregame starter run-prevention input is committed. Prerequisites named. Over/under stays PAUSED |
| P318 NFL passing yards (decomposed) | **STOP** at the calibration bar | `data/internal/research/nfl/reports/passing-yards-decomposed-dev.{md,json}` | MAE 60.5–61 vs 65.2/65.3 baselines, level 1.007, coverage in band, ECE 0.050–0.051 → not cleared; not registered; ESTIMATE stays |
| P317 engine-level shadow | FORWARD SHADOW, accumulating | rows `data/internal/research/mlb/engine-level-shadow/2026-09-15.json` (15 games); receipt appears after the first nightly settle | research only; unrelated to call eligibility |
| P305-F EPL totals shadow | registered + wired | first rows Thu 2026-09-17 21:00Z | research only |
| NFL share-level forward | Week 2 file committed once, on time (`780b35c4c`) | boards publish `nfl-player-share-level-v1` for receptions / receiving / rushing; passing yards ESTIMATE | live, receipt-gated |

## E. Model status table — unchanged (scorecard 2026-09-15T14:29Z, `changes: []`)

NFL game forecasts EXPERIMENTAL · NFL player ranges FORWARD_TEST (pass yds ESTIMATE) · NFL TD HOLDING (168) · MLB winner WATCH (615) ·
MLB totals **PAUSED** (586) · MLB run line HOLDING (615) · EPL match model VALIDATED, forward 0/60, live TOO_EARLY (36) · Ligue 1 (9) ·
UFC fight model VALIDATED, live TOO_EARLY (31). No family reads Paused without the gate; no transition has occurred yet.

## F. Test evidence (last full gate, final tree)

```
lint:scripts                       exit 0
run-suite --phase unit             # pass 6264  # fail 0
tsc --noEmit                       exit 0
build                              exit 0   (/ 260 KB · /simulate 576 · /today 708 · /mlb 1,896 · /ufc 616 · NFL game ~224)
run-suite --phase post-build       # fail 0 (529 checks)
build-route-inventory              73 routes · findings 0 (P0 0)
homepage words                     live 1,658 · busiest fixture 1,811 (ceiling 1,820)
```
Browser (built export): homepage desktop + 375px no overflow; `/today` following filter with a followed club → "1 of 15 games",
row kept and marked; `/ufc/bout/…` story controls 44px at 375px; NFL game page at 390px Save 44px (only tables overflow inside their
own scroll containers); no console errors on any page.

## G. Known defects / debts (real)

1. **Busiest-day headroom is 9 words** (1,811 of 1,820; the ≤1,700 target was not reached). Severity: medium. Next trims: the
   Featured-simulations cards' run-count label (5 cards) or the flagship product blurbs; never predictions.
2. **`/mlb` hub is 1,896 KB** (budget passes, shrink-only). Severity: medium (phone load). Next: per-row inline styles → classes.
3. **P330 labels not yet on the committed artifacts** until the 21:00Z event window regenerates status + index together. Low.
4. MLB over/under differentiation: blocked on data (historical finals for a multi-season park table; a registered starter capture). High for the paused family; research only.
5. P318: a calibration-shaped iteration is the only path left; declare it before any look. Medium.
6. `/saved` not in the mobile bottom bar (rail + footer + menu; the bar's budget was audited, not changed). Low.
7. `nfl-event-window` dispatches can be cancelled while PENDING in the shared `gtp-generated-artifacts` group; `-f skip_odds=true` re-dispatch is zero-credit. Low.
8. `PA_PER_GAME` doc defect (hits do not reproduce the board by construction; realised 4.27 PA per slot). Low.

## H. Time-sensitive follow-ups

- **~05:30Z / 06:00Z nightly-settle**: `data/internal/research/mlb/engine-level-shadow/receipt.json` appears with `graded > 0`
  for 2026-09-15 finals, state ACCUMULATING (needed 200); the morning production writes `engine-level-shadow/2026-09-16.json`.
- **21:00Z tonight** `nfl-event-window`: `model-status.json` gains `window` and PUBLISHED family rows; `index.json` follows;
  `product-eligibility.json.indexGeneratedAt` equals the index stamp. The `family-publication` LIVE test asserts this.
- **Wed 13:30Z** `nfl-props-forward-shadow`: the Week 2 file must stay one commit (`780b35c4c`).
- **Thu 21:00Z** `epl-matchweek`: private rows carry `shadowTotals`, public rows do not, `forward-totals/receipt.json` ACCUMULATING.

## I. Launch readiness (P337)

Product truth: featured forecasts carry owner, stamp, status, settlement ✓ · no paused probability leaks ✓ · NFL labels derive from
boards ✓ (visible after 21:00Z) · P305-F private ✓ · P317 ≠ call eligibility ✓. Reliability: busiest fixture ✓ · no broken routes ✓
· archived NFL routes persist ✓ · saved lifecycle tests ✓ · period-scoped guards ✓. UX: 375/390 clean ✓ · 44/36 targets ✓ · reduced
motion by pure rules ✓ · refusal states explain ✓. Research: forward receipts from artifacts ✓ · P331 no-go before any target
scoring ✓ · P318 stopped honestly ✓ · no rejected model promoted ✓. Operations: workflows scoped ✓ · public/private pinned ✓ · merge
never rebase ✓ · prod verified after every batch ✓.
**No P0.** P1: busiest headroom (G1), `/mlb` weight (G2). P2: G3–G8.

## J. Founder decisions still required

1. UFC historical data source (unchanged).
2. Whether to commit historical MLB finals (three or more seasons) so a multi-season park table can be built — the prerequisite for any totals differentiation candidate.
3. P317 adoption only if its receipt reads ENGINE_LEVEL_PASS at n ≥ 200 (nothing to decide yet).

## K. Next 5 highest-value tasks

1. Verify §H windows (tonight's first shadow receipt; the 21:00Z labels; Thursday's P305-F rows).
2. `/mlb` hub page weight (inline styles → classes) and 20–30 more homepage headroom words.
3. Founder decision on historical MLB finals → multi-season park table → registered differentiation candidate.
4. P318 calibration iteration, declared before the look.
5. `/saved` phone discoverability without a bottom-bar change.
