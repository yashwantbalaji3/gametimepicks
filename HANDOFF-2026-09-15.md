# GameTime Picks — Engineering Handoff (2026-09-15, Phase 3, ~16:00 UTC)

Supersedes the Phase 2 (07:40 UTC) version of this file. For a new Claude Code session at `/Users/yashwantbalaji/Downloads/gametimepicks`.
Read §A first, then the memory index (`~/.claude/projects/-Users-yashwantbalaji/memory/MEMORY.md`, especially
`gtp-program-317-mlb-engine-level`, `gtp-phase3-save-archive-guards`, `gtp-command-center-contract`, `gtp-simulation-story`,
`gtp-phase2-saved-brief-analytics`). The standing charter is unchanged (no paid data, no protected-record edits, no wagers, no social
posts, never promote a REJECTED model, never `git add -A`, never commit `vp/**` / `HANDOFF-2026-08-20.md` /
`docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, merge never rebase, batch `app/` pushes). There is no `AGENTS.md` in the repo.

## A. Production state

| item | value |
|---|---|
| pushed this phase (in order) | `7df4a19f9` P317 engine params + study + registration · `c11ad2d0e` P317 shadow wiring · `1207df43d` P319/P320/P321/P322 + guard repairs · `f12f0ba1f` P323/P324/P325/P327 + wording (merge `d597f0208`) · `8c9f1ffb1` weekly-boards guard scoped to the period · `0b4060197` slate rows carry full club names (P323 fix) |
| verified live in the browser | `0b406019` (the final push, built 16:13Z) is what production serves; earlier checks on `4998c8d9` (archived DEN@KC page + story, DET@BUF Save) and `d597f020` (bout story on `/ufc/bout/401903509/`, 15 slate rows carrying team markers on `/today`) passed with no console errors. Read `data/build-info.json` `commit.shortSha` in the in-app browser (curl hits the Vercel checkpoint) |
| CI | quality-gate **green** on `1207df43d` (`34987858443`). `d597f0208` (`34990941059`) went RED on a LIVE weekly-boards guard that globbed last week's frozen boards — fixed in `8c9f1ffb1` (its run `34991596578` was cancelled by the newer push; the workflow cancels in-progress runs on main). Final: `0b4060197` run `34993477865` — completed success |
| founder-owned untracked | `HANDOFF-2026-08-20.md`, `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md`, `vp/**`, `GameTimePicks_Claude_Code_Master_Prompt_Phase_3_2026-09-15.md` — leave them |

## B. What changed for users this phase (all pushed)

1. **Save this forecast on report pages** (P319): MLB report (under the hero), NFL game, EPL match and UFC bout pages — the SAME
   card the homepage features (exported builders), same settlement key. Refused for a paused/withdrawn call and for a started
   event (client clock, "Started · not saveable"); a paused-call snapshot no longer joins the first row of any market.
2. **A played NFL game keeps its page and an archived pregame story** (P320): the exact frozen revision the graded record names
   (dated file or per-revision receipt; never a neighbour), labelled "Archived pregame read · published before kickoff", the
   final printed beside it from the record; `/results/nfl` rows link each archived read. 32 NFL game pages build (16 + 16).
3. **Touch targets** (P321): story controls and report Save 44px; compact card controls, saved-list and Model Lab links 36px.
4. **Busy-day copy trims** (P322): How-it-works, parlay and top-reads explainers shortened (~90 words); predictions untouched.
5. **Only teams I follow** on `/today` (P323): browser-only slate filter + a "★ following" mark on followed teams' rows.
6. **Per-bout UFC story** (P325) on every bout page, from the same bout object the card walkthrough reads.
7. **Wording**: negated "beat the market" sentences at their sources now read "out-predict the sportsbook market".
8. Analytics vocabulary: `placement` on saves, `archived_story_opened`, `following_filter_used` — provider still OFF.

## C. Canonical owners (new or changed)

- Card builders: `app/src/lib/command-center/featured.ts` (`cardFromMlbPrediction/NflEvent/EplRow/UfcBout`, lifecycle from the clock);
  report context: `app/src/lib/command-center/report-card.ts` (`reportCardContext`, `leadStatus`, `LEAD_STATUS_ID`).
- Saved: `saved-schema.mjs` (`saveEligibility`, `savedAfterStart`, `saveCardOf`), `saved-store.ts` (`SaveCard` type), `results.mjs`
  (fail-closed families). Integrity suite `saved-integrity.test.mjs`.
- Archived NFL: `app/src/lib/sports/nfl/archived-forecast.ts` (+ receipts at `data/internal/nfl/forecast-receipts/<date>/<id>-rev-*.json`).
- Engine: `app/src/lib/mlb/full-game/engine.ts` (`EngineParams`, `DEFAULT_ENGINE_PARAMS` = published engine byte for byte),
  `engine-candidates.ts` (pinned to the protocol), generator writes the private shadow.
- UFC bout story: `buildUfcBoutPresentation` in `lib/simulate/presentation/ufc.ts`. Following: `components/today/{followed-mark,following-filter}.tsx`.
- Guards repaired: `events/read-model.test` (period KIND), `sports/nfl/gameday-evidence.test` (scoped to the evidence window),
  cross-surface date drift (ISO dates beside "slate" — render ET words), NFL outcome chapter sentence.

## D. Research / model work

| program | state | evidence | verdict / public |
|---|---|---|---|
| P317 MLB totals engine level | dev study DONE (second look, 204 rebuilt games) · **forward-only private shadow REGISTERED and LIVE** | `data/internal/research/mlb/reports/{totals-engine-prior-sensitivity.json,.md,engine-level-shadow-protocol.json}`; first rows `data/internal/research/mlb/engine-level-shadow/2026-09-15.json` (15 games; control mean 8.11 vs candidate 8.74) | level repairable with documented league rates (7.81 → 8.43); the **over/under call fails on SLOPE** (0.21 vs 1.32) — no call candidate, pause stays; receipt `engine-level-shadow/receipt.json` from nightly-settle (n ≥ 200, one verdict) |
| P318 NFL passing yards | **NOT STARTED** this phase | design note only | ESTIMATE stays; dev exploration per the note's bars is the next research task |
| P305-F EPL totals shadow | registered + wired | first rows Thu 2026-09-17 21:00Z | research only |
| NFL share-level forward | Week 2 file COMMITTED ON TIME (`780b35c4c`, 14:53:26Z; first kickoff Thu 00:15Z) — 3,215 rows, p25/p75/espnId present, anytime_td 1,106, `earlierGamesWithoutStatRows` [] | boards rebuilt 15:45Z with `nfl-player-share-level-v1` PUBLISHED for receptions / receiving yds / rushing yds; passing yds ESTIMATE | live, receipt-gated |

## E. Model status table — unchanged (scorecard 2026-09-15T10:06Z, `changes: []`)

NFL game forecasts EXPERIMENTAL · NFL player ranges FORWARD_TEST (pass yds ESTIMATE) · NFL TD HOLDING (168) · MLB winner WATCH (615) ·
MLB totals **PAUSED** (586) · MLB run line HOLDING (615) · EPL match model VALIDATED, forward 0/60, live TOO_EARLY (36) · Ligue 1 (9) ·
UFC fight model VALIDATED, live TOO_EARLY (31). No family reads Paused without the gate; no transition has occurred yet.

## F. Test evidence (last full gate, batch-2 tree)

```
lint:scripts                       exit 0
run-suite --phase unit             # pass 6258  # fail 0
tsc --noEmit                       exit 0
build                              exit 0   (/ 1,766 words vs 1,820 ceiling; 32 NFL game pages)
run-suite --phase post-build       # pass 529  # fail 0
build-route-inventory              73 routes · findings 0 (P0 0)
```
Browser (built export, port 4173): archived DEN@KC page + story; DET@BUF Save round trip (44px, aria-pressed, stored with
sourceRoute) → `/saved` lists it; MLB report / UFC bout Save 44px; EPL archived match "Started · not saveable"; homepage at 375px
no horizontal overflow, compact Save 36px; no console errors. Prod smoke on `4998c8d9` as in §A.

## G. Known defects / debts (real)

1. **Busiest homepage state still projected over the ceiling** — 1,766 today with three lanes; a four-lane evening ≈ +100 lane
   words +69 started chips ≈ 1,935 > 1,820. Severity: high (the ceiling guard will fail on that day). Next trims: the Featured
   simulations block (5 cards ≈ 150 words) and nav-rail descriptions; never the predictions. No synthetic busy-day fixture test exists yet.
2. **NFL index `playerFamilies` labels lag the boards** — the index says Rushing yards ESTIMATE_NEAR_BAR / Receiving MODEL_READY while
   the boards publish `nfl-player-share-level-v1` for both (`app/scripts/nfl/build-nfl-index.mjs`). Severity: medium (cross-surface).
3. **P318 passing yards** not explored. Severity: medium.
4. **MLB over/under differentiation** — the slope defect needs a candidate with a defensible input (park run environment from committed
   linescores by venue; pitcher run prevention beyond strikeouts). Severity: high for the paused family, research only.
5. **`PA_PER_GAME` doc defect** — the engine doc claims hits reproduce the board by construction; realised PA/slot is 4.27 not 3.85, so
   simulated hits run 11% above Σ projections. The protocol keeps the divisor as published and says so. Severity: low (documentation).
6. `/saved` not in the mobile bottom bar (rail + footer + menu reach it; the bar's character budget was audited, not changed). Low.
7. LIVE guards that glob `player-board/*.json` must scope to the period now that played weeks keep their boards (two hit today: designated-out and weekly-boards family). Low; pattern recorded in memory.
8. `nfl-event-window` dispatches can be cancelled while PENDING in the shared `gtp-generated-artifacts` concurrency group (one was,
   at 15:21Z); the re-dispatch at 15:43Z ran. The 15:00Z schedule never fired. Low; known P257 class.
9. No service worker (deliberate). Not a defect.

## H. Time-sensitive follow-ups

- **Tonight (nightly-settle, ~06:00Z)**: first `engine-level-shadow/receipt.json` — expect `graded > 0` for 2026-09-15 finals,
  state ACCUMULATING (needed 200). Also `engine-level-shadow/2026-09-16.json` from the morning production run.
- **Wed 13:30Z** `nfl-props-forward-shadow`: nothing to grade yet; the Week 2 file must NOT change (one commit, `780b35c4c`).
- **Thu 21:00Z** `epl-matchweek`: private rows carry `shadowTotals`, public rows do not, `forward-totals/receipt.json` ACCUMULATING.
- **CI on the final push** (`34993477865`) if it read anything but success above: the local gate on the same tree was green (unit 6,257/0, post-build 529/0).
- **Sat evening**: the four-lane homepage word count (debt 1).

## I. Founder decisions still required

1. UFC historical data source (unchanged).
2. Whether the P317 engine-level candidate may be adopted for the public simulation IF its forward receipt reads ENGINE_LEVEL_PASS
   at n ≥ 200 (nothing to decide until then; the over/under pause is not touched by it either way).

## J. Next 5 highest-value tasks

1. Verify §H windows (tonight's first shadow receipt; Thursday's P305-F rows; batch-2 deploy).
2. Homepage busiest-state trims + a synthetic four-lane fixture test (debt 1).
3. MLB differentiation candidate design (debt 4) — park run environment from committed linescores, registered before any look.
4. P318 passing-yards dev exploration per the design note's bars.
5. NFL index family labels from the boards' publication (debt 2).
