# v1.7 — Workflow audit (Phase 13.1 · Lane C, 2026-09-22)

Scope: `.github/workflows/{daily-products,nightly-settle,mlb-daily-production,quality-gate}.yml` and the commit
helper `scripts/ci/commit-generated.sh` they call. Every finding cites file:line as of this branch; fixed items
carry a comment in the file naming the W-number below. Related: `docs/V17_SHADOW_INTEGRITY_AUDIT.md` (I1–I9).

## Findings

| # | Workflow · line | Class | Finding | Status |
|---|---|---|---|---|
| W1 | daily-products.yml:353 → `scripts/ci/commit-generated.sh:30` | retry that cannot succeed | The helper's rebase was `git rebase -q origin/main >/dev/null 2>&1`. The commit step first un-stages stamp-only files (`git restore --staged`, daily-products.yml:339-342), which leaves them MODIFIED in the worktree; `git rebase` then refuses ("cannot rebase: You have unstaged changes") behind the redirect, and the loop pushes the same stale HEAD five times. **Observed:** run **35630279965** (2026-09-21 17:10Z) — `stamp-only, not published: app/public/data/parlays/coverage-matrix.json`, then five `! [rejected] … non-fast-forward` with no conflict line, `push never landed after 5 attempts`, while only ONE commit (`412aff557`, 17:11:24Z) had landed on main in the window. Reproduced in a scratch repo (dirty tracked file → rebase exit 1; `--autostash` → rebase succeeds). | **FIXED** — `git rebase -q --autostash origin/main`. Test: `app/src/lib/ops/commit-generated.test.mjs` W1 (real git, two clones, an unstaged stamp file; lands on attempt 2). |
| W2 | daily-products.yml:353 · `commit-generated.sh:19,31-38` | historical selection rewritable on retry | `GENERATED_PATHS` includes `data/internal/products/`, and the helper resolves any conflict inside it to "this (later) run's copy". A shadow day file is first-publication-wins; two runs that both created it would have been resolved to the later one — the S7 defect on the commit path. | **FIXED** — `PROTECTED_RE` now also refuses `data/internal/products/selector-shadow/` (fails loud, nothing forced). Test: commit-generated.test.mjs W2 (first publication on origin byte-identical after the refused run). |
| W3 | daily-products.yml:303-307 | bot commit could silently erase a receipt from the commit | `git add a/ b/ c/ … \|\| true` — git stages NOTHING when any single pathspec matches no file (`fatal: pathspec 'x/' did not match any files`, reproduced), so one absent directory would have dropped every sibling on the line (including `selector-shadow/`) and `\|\| true` hid it. Latent today (every directory exists) — the class that made P264 "green-but-broken". nightly-settle.yml already uses one path per line (646-749). | **FIXED** — one path per `git add` (daily-products.yml, comment W3). |
| W4 | nightly-settle.yml (pre-fix 309-317) | first-publication immutability defeated by timing | The roll-time shadow publication read a slate that does not exist until ~10:00Z (audit I1). Also wrote `products/availability/latest.json` for today at 01–06 ET with 0 MLB legs, whose public reason would have read "no events" / "not eligible" for a slate whose prices simply had not been captured yet — a false public claim. | **FIXED** — the step now only grades and rolls; daily-products is the one publisher (after its pool gate, daily-products.yml:168-175). |
| W5 | nightly-settle.yml:92-94 vs daily-products.yml:72-74 | interleaving writers | nightly-settle uses concurrency group `nightly-settle`; daily-products and mlb-daily-production use `gtp-generated-artifacts`. `workflow-failure-visibility.test.mjs:96-99` pins the shared queue for five writers but lists neither nightly-settle nor daily-products. With W4 fixed, the two no longer write the same shadow paths at the same hour (nightly grades ~05–10Z, daily publishes ~10–17Z); a residual overlap is caught by W2 (fail loud) and by nightly-settle's own `git pull --rebase --autostash` (add/add → `exit 1`, nightly-settle.yml:769). | RECORDED, not changed — moving nightly-settle into the shared queue is a scheduling decision (it heads the morning chain, nightly-settle.yml:67-69) beyond a minimal diff. |
| W6 | quality-gate.yml:15-43 | gate path filter vs bot-written public data | `push.paths` deliberately excludes `app/public/data/**` (comment at :10-12) so bot commits do not trigger a 17-minute build. That leaves one path by which an internal artifact could land under `app/public/data/products/` with no guard running: a bot commit. | **FIXED** in daily-products.yml — new step "Public data boundary" runs `app/src/lib/products/public-data-boundary.test.mjs` on the working tree before the commit step (fail loud). Mutation-probed: a fake `public/data/products/selector-shadow/x.json` and a fake `data/internal/products/selector-shadow` read under `app/src` each turn the guard red. |
| W7 | quality-gate.yml:15-49 | gate path filter omits the workflows it pins | `src/lib/workflow-failure-visibility.test.mjs`, `lifecycle-automation.test.mjs`, `daily-production-commit-scope.test.mjs`, `sports/ufc/fightday-watch.test.mjs` etc. assert the text of OTHER workflow files, yet only `.github/workflows/quality-gate.yml` was in the push/PR path lists — an edit to nightly-settle.yml or daily-products.yml pushed to main ran none of them (the memory's v1.6 `app/api/**` omission, same class). | **FIXED** — `.github/workflows/**` added to both `push.paths` and `pull_request.paths`. |
| W8 | daily-products.yml:270 | `\|\| echo` masking | `build-risk-coverage.mjs … \|\| echo "::warning::coverage matrix reported MISSING cells"` — a crash in the builder is indistinguishable from a MISSING-cell report (P264 shape). Pre-existing (P200), not v1.7. | RECORDED — not changed: the builder uses its exit code to mean "MISSING cells", so the fix is in the script's contract, not the workflow line. |
| W9 | nightly-settle.yml:234-235, 247, 270, 348, 457-487, 507-576 | `\|\| echo "::warning::…"` on 17 steps | Each is a deliberate "prior artifact retained" degradation; the Rule S fold (280-287), the roll (297-303), the shadow grade (309-315), the health gate (578-586) and the commit are NOT masked. The shadow step has `set -euo pipefail` and no `\|\| true`. Job-level `continue-on-error` absent (pinned, workflow-failure-visibility.test.mjs:38-47). The one masked step that matters to v1.7 surfaces: `settle-ladder-cards.mjs \|\| echo` (:270) — run 35622690908 shows no warning line, and `products/lifecycle/latest.json` is dated 2026-09-07 because the two stores it settles froze on 2026-08-17 (handoff §5 gap 1), not because the step failed. | RECORDED (no v1.7 defect). |
| W10 | mlb-daily-production.yml:274-341 | capture archive commit + reset --hard | The team-market ingest archives every capture under `team-markets/captures/<date>/` (`ingest-mlb-team-markets.mjs:216`); the persist step stages `app/public/data/mlb/team-markets/` (:286), which includes the archive. Its retry does `git reset -q --hard origin/<ref>` then re-applies ONLY the files of its own commit (:327-330) — it cannot drop `data/internal/products/**` (never staged, never touched). Forbidden-path assert (:288-293) fails loud. `\|\| true` on the `git add` line (:286) has the W3 atomicity class (fifteen pathspecs on one line) — every directory exists today. | RECORDED — W3-class latent; not in this lane's minimal diff for a workflow it does not own beyond the archive step. |
| W11 | daily-products.yml:144-161 | a no-op distinguishable from a failure | Recovery guard skips only on `schedule` when the dated receipt carries a completed evaluation (P247); "no change — products already current" exits 0 with its own line (:308-311); "every staged artifact differed only by generatedAt" exits 0 with its own line (:344-348); the shadow builder prints `already published … nothing rewritten` or `INPUTS_MISSING … not published` (distinct lines, exit 0) vs a crash (non-zero, `set -e`). | HOLDS. |
| W12 | daily-products.yml:97-100 | trust boundary on workflow_run | Only a successful producer run from the default branch may chain; the consumer checks out the default branch and reads one committed artifact validated by the pool gate. | HOLDS (unchanged). |

## Shell-guard coverage of the v1.7 steps

`app/src/lib/workflow-failure-visibility.test.mjs` (job-level `continue-on-error`, failure notifier, shared
queue, push-failure shape, YAML sanity) — 49/49 pass after the edits, together with
`sports/ufc/fightday-watch.test.mjs`, `ci/quality-gate-contract.test.mjs`, `daily-production-commit-scope.test.mjs`,
`lifecycle-automation.test.mjs`, `gate-parity.test.mjs`, `launch/schedule-cadence.test.mjs`. The three edited
workflows parse (`yaml.safe_load`: jobs `generate` / `settle` / `quality,python`). `npm run lint:scripts` clean.

## Diff summary (this lane)

- `.github/workflows/nightly-settle.yml` — shadow step grades only; the two roll-time publish lines removed; comment cites I1/I2.
- `.github/workflows/daily-products.yml` — `git add` one path per line (W3); new "Public data boundary" step before the commit (W6).
- `.github/workflows/quality-gate.yml` — `.github/workflows/**` in push + pull_request paths (W7).
- `scripts/ci/commit-generated.sh` — `--autostash` (W1); `selector-shadow/` refused for auto-resolution (W2).
- `app/src/lib/ops/commit-generated.test.mjs` — two new real-git cases (W1, W2). 5/5 pass.
