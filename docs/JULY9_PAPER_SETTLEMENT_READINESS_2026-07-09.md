# July 9 Paper Settlement Readiness (2026-07-09, late night)

**Outcome: B — the July 9 paper cards stay honestly `pending`.** The slate is not fully final in any
committed source, so nothing is settled and no volatile cache is committed. Official money untouched
(md5 `affe6b21…`, 19-14, $0).

---

## Final-data status (inspected ~07-09 23:55 ET)

| source | coverage | 07-09 status |
|---|---|---|
| MLB linescore cache (`data/internal/mlb/linescores/`) | 07-04…08 | **absent** for 07-09 |
| MLB `settled_leans.jsonl` (player-prop actuals) | 05-16…07-08 | **0 rows** for 07-09 |
| WC settlement (`world-cup/settlement/`) | 06-11…06-26 | **absent** for 07-09 |
| StatsAPI schedule (live, read-only inspection) | live | **12 Final / 1 Live** of 13 |

### Classification

- **MLB July 9: `partial_final`** — 12 of 13 games Final, **1 still Live** (per a read-only StatsAPI
  inspection). Not fully final.
- **Soccer July 9: `partial_or_missing`** — no committed `world-cup/settlement/2026-07-09.json`; the BB
  legs (France vs Morocco, Spain vs Belgium) have no committed FT result.

## Decision (per the guardrails)

- **No live-fetch commit.** The conditional `--fetch-final` path requires the *entire* slate to be final;
  it is partial (1 Live game), so committing a fetched linescore would be a **partial volatile cache** —
  explicitly disallowed. StatsAPI was used for **inspection only**, nothing fetched was written.
- **No partial settlement committed.** Even the 12 final games are not cached, because a partial-day cache
  must not be committed as if final.
- **Cards remain `pending`.** Re-running committed-only settlement is idempotent: both cards stay
  `pending/pending`, `paperPnlUnits: 0` — WROTE 0, skipped 2. Pending is **not** a loss.

## What settles this later (a future mission)

Once **all 13** July-9 MLB games are final and the deterministic caches are committed
(`data/internal/mlb/linescores/2026-07-09.json` via `fetch-mlb-linescores.mjs`, and `settled_leans.jsonl`
regenerated by the MLB grading pipeline for player props), and once
`world-cup/settlement/2026-07-09.json` exists for the soccer legs, re-run
`settle-paper-product-cards.mjs --date 2026-07-09 --write` to resolve the cards from committed finals.

## Money / boundary posture

No money artifact read/written; md5 `affe6b21…` verified unchanged before/after. No exposure, no public
change, no full-game-sim driving. All artifacts internal (`data/internal/`, 404 on prod).
