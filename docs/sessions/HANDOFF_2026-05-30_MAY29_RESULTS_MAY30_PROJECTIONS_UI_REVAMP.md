# Handoff — 2026-05-30 overnight runbook

**Author:** autonomous overnight session (Claude).
**Window covered:** May 29 settlement → May 30 active slate + a full
UI/UX revamp pass.
**Branch state at handoff:** `main` synced through the Phase 11 merge
(`1a77adf`). All twelve runbook phases complete.

> Every number in this doc traces back to committed JSON regenerated by
> the pipeline. Nothing here re-grades a settled slip, alters a pregame
> suggestion from its own final result, or claims a calibrated hit rate.
> The product copy stays "experimental · publicly tracked."

---

## 1. TL;DR

- **May 29 is settled** (MLB-weighted slate) and reflected on `/results`.
- **May 30 is the active slate** — optimizer snapshot present (120 slips;
  all four public risk sections populated 4/4/4/4), NBA + MLB boards in
  place.
- **Public-era record (05-27 → 05-29):** 36W–146L, 182 decisive, **19.8%**
  slip hit rate. This is what the Results hero renders. It is below what
  the combined odds in these lanes need to clear; stated plainly, not
  dressed up.
- **Five PRs shipped this session** (shell/nav, Parlay Lab, Results, Bank
  Builder, learning notes) + **one in flight** (leg-quality gates).
- **No promotion / demotion taken.** Sample is still below every
  `docs/MODEL_LEARNING_LOOP.md` §7 gate.

## 2. Data state

| Surface | State | Evidence |
|---------|-------|----------|
| May 29 settlement | Complete (decisive denominator stable) | `optimizer-summary.json` byDate has `2026-05-29` |
| May 30 active slate | Live | `app/public/data/parlays/optimizer/2026-05-30.json` (totalSlips 120, publicRiskSections 4/4/4/4); `boards/2026-05-30.json`, `mlb/boards/2026-05-30.json` |
| Results hero (public era) | 36-146, 182 decisive, 19.8% | era-filtered byDate sum in the loader |
| `optimizer-summary.json` lifetime | 44-188, 232 decisive, 18.97% **(includes pre-era 05-25/05-26)** | the `/results` UI applies the 2026-05-27 era filter at the loader, so the *public* hero excludes the two pre-era slates |

Pre-era slates (05-25, 05-26) remain on disk as an internal archive and
are filtered out of every public surface by `public-parlay-era.ts`.

## 3. PRs this session

| Phase | PR | Title | Merge |
|-------|----|-------|-------|
| 3 | #187 | fix(results): resolve gradable pending parlay slips | `a4ad74f` |
| 6 | #188 | fix(ui): consistent page hero + nav naming | `64751a6` |
| 7 | #189 | fix(parlay-lab): filter summary line + tray clearance | `6bcd935` |
| 8 | #190 | fix(results): simplify results dashboard | `e32174b` |
| 9 | #191 | fix(bank-builder): never surface a graded leg as a live Builder Slip | `f1f23bf` |
| 10 | #192 | docs(audit): update learning notes after May 29 settlement | `d035744` |
| 11 | #193 | docs(parlays): define leg-quality risk gates | `1a77adf` |

Every merge went through the Vercel gate (real `Vercel – gametimepicks`
green + `mergeStateStatus: CLEAN`) before squash-merge + `main` sync.

## 4. UI/UX revamp summary

- **Shell / nav (PR #188):** consistent page hero + nav naming across
  Parlay Lab, Results, Home, Bank Builder, Events.
- **Parlay Lab (PR #189):** a `FilterSummaryLine` ("Showing N parlays …")
  whose count is derived from the SAME `countDisplaySlips` source the
  cards render from, so the summary can never disagree with the spread;
  shared `getDisplaySectionBuckets` helper; bottom-tray clearance fix.
- **Results (PR #190):** promoted section eyebrows to real `<h2>`s
  (a11y), per-pending-slip caption explaining *why* a slip is pending
  ("Waiting on N of M legs" / "All M legs pending"), deleted the dead
  `FreshEraStatusBlock`.
- **Bank Builder (PR #191):** a pure `_builderHasGradedLeg` guard so a
  pending slip that already contains a decided-result leg is never shown
  as a live "Builder Slip." Paper-trading / educational framing intact.

## 5. Learning + risk-quality docs

- **`docs/LEARNING_NOTES_2026-05-29_SETTLEMENT.md`** (PR #192) — results-
  only. Records the per-date + era record, `byPublicSection` and
  `bySportBucket` splits, and the honest takeaways (risk ordering held
  on 05-28; MLB dragged both days; 05-29 was a thin MLB-heavy slate).
  **No model change.**
- **`docs/PARLAY_LEG_QUALITY_GATES.md`** (PR #193) — defines the per-leg
  eligibility gate across all four profiles, separates it from the slip-
  level correlation caps, and records the finding that the public
  Low/Medium/High/Longshot spread inherits a single **aggressive**-gated
  leg pool — section = combined odds + leg count, not a per-leg quality
  bar. Ships a pure, inert `leg-quality-gates.ts` explainer mirror (21
  tests) and a **PROPOSED, un-shipped** per-section leg ladder gated
  behind the promotion path + operator approval.

## 6. Verification gates met

- `npx tsx --test src/lib/*.test.mjs` — **535 pass** (was 511 at session
  start; +21 leg-quality, +3 builder-guard).
- `npx tsc --noEmit` — clean.
- `npm run build` — static export EXIT 0.
- Banned-copy scans clean on every shipped surface (only acceptable
  matches: CSS `inline-block` substrings, the required Bank Builder "We
  do not take real money" disclaimer, and the "no guaranteed payout"
  honesty negation).

## 7. Open follow-ups (NOT done — for the next operator)

1. **Backfill `byPublicSection` for 05-27** — that date persisted `{}`;
   the section-level learning signal is missing a day.
2. **MLB underperformance** — recorded across 05-28/05-29 but NOT acted
   on. Only de-weight through the documented rolling-window path
   (`MODEL_LEARNING_LOOP.md` §3) with explicit operator approval — never
   off two slates.
3. **Proposed per-section leg ladder** (`PROPOSED_SECTION_LEG_GATES`) —
   would make "Low Risk" mean conservative-grade legs. Needs out-of-
   sample availability confirmation + a pinning test + operator approval
   before wiring into `generate_public_risk_sections`.
4. **Keep accumulating decisive slips** toward the §7 gates (100+/lane,
   200+ overall) before any public hit-rate claim.

## 8. Hard gates honored throughout

No fabricated outcomes/odds/stats/parlays/hit-rates; no manual outcome
edits; no using final results to alter same-slate pregame suggestions; no
pre-era (05-25/05-26) hit-rate leakage; no new sports
(WNBA/UFC/FIFA/cricket/IPL) projections/parlays/grading; no audit policy
consumed by the optimizer without operator approval; Bank Builder stays
paper-trading/educational; no banned promo copy. The ~52 untracked root
working notes were left untracked — only the specific deliverable files
were staged per PR.
