# GameTime Picks — Engineering Handoff (2026-09-17, v1.4 MATCHUP EXPLORER + COMPARE, ~17:15 UTC)

Supersedes `HANDOFF-2026-09-17-v1.3.md` for current state. Read this, `docs/MATCHUP_COMPARE.md` (v1.4 contract + runbook),
`docs/TEAM_PLAYER_RESEARCH.md`, `docs/GAMETIME_DATA_PLATFORM.md`, `docs/GAMETIME_LIVE.md`, `docs/RETENTION_ARCHITECTURE.md`.
Charter unchanged (no paid data, no new provider, never promote REJECTED/STOP/PAUSED, never `git add -A`, merge never
rebase, never commit `vp/**` / `HANDOFF-2026-08-20.md` / `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / pasted prompts).

## ⭐ NEXT SESSION — START HERE (v1.5 Research Lab — not started)

1. UTC + ET; `git fetch origin`; fast-forward (bots push `main` constantly).
2. From `app/`: `node scripts/data-platform/build.mjs --all --check` (STALE → `--all`, `validate.mjs`, `parity.mjs`) →
   `node scripts/research/build-research-projections.mjs` + `--check` → `node scripts/compare/build-compare-projections.mjs`
   + `--check`. Commit `data/internal/platform/v1`, `data/research-projection/v1`, `data/compare-projection/v1` explicitly.
   The compare builder REFUSES to drop a published matchup (exit 2) — investigate, never force.
3. Verify by behaviour: `/compare/players/nfl/?a=keenan-allen&b=travis-kelce` composes (3 `/data/compare/v1/` fetches,
   0 Live); `/compare/players/ufc/` 404; `/matchups/nfl/401872656/` 200; `/api/live/?sport=nfl` UNSUPPORTED_SPORT.
4. ⚠ CI quality job is 21m14s (limit 25). Optimize browser a11y / build before adding routes (see §M).

## A. State

| item | value |
|---|---|
| release commits | `e575be1d7` data refresh · `5506cda38` contracts + projection · `66ccac4e0` pages/discovery/SEO/guards · `caed6ff2f` probe gaps · `1142c789c` CB7 chunk scan · `8b0c663a4` B1 self-trip fix · `8f0abd371` docs |
| CI | quality-gate **35248388319 on `8f0abd371` — success**; quality 21m14s (unit 222 s · build 261 s · rendered 241 s · browser install 52 s · browser a11y 403 s) |
| local gate | tsc 0 · lint:scripts 0 · unit **6,685 / 6,684 pass / 0 fail / 1 skip** · build 0 · post-build **573 / 570 / 0 fail / 3 skip** · route inventory **83 / 0 findings** · structural a11y 0 · browser a11y compare+matchup+research **48/48 (3 engines)** |
| production | serves `81f54e165` (bot descendant of `8f0abd371`); all compare/matchup routes 200, UFC compare + unknown matchup 404, prior routes healthy, MLB Live events, NFL UNSUPPORTED_SPORT; sitemap 1,465 (6 compare shells, 33 NFL matchups, 0 MLB matchups, 0 query URLs, 0 templates) |
| research receipts | P317 ACCUMULATING 30/200 · P305-F not due · NFL props forward last success 09-16 — classified only |
| verdict | **MATCHUP EXPLORER + COMPARE v1.4 — PUBLIC AND VERIFIED** |

## B. Coverage (`data/compare-projection/v1/readiness.json`)

| Sport | Team Compare | Player Compare | Matchup pages |
|---|---|---|---|
| MLB | 30 SHIPPED · H2H 435 pairs / 8,136 meetings (since 2023) | 255 SHIPPED (captured categories; 3 usable families) | 93 noindex |
| NFL | 32 SHIPPED · H2H 496 pairs / 7,269 meetings (since 1999) | 720/750 SHIPPED · 12 families · seasons ≤2025 | 33 indexable (all with a join to `/nfl/game`: MF1) |
| EPL | BLOCKED (no id-keyed results) — noindex explanation page | 414 SHIPPED · 9 families | none |
| UFC | n/a | BLOCKED (no comparable family) | none |

## C. Decisions a later session must not undo silently

- Compare reads the RESEARCH projection only; B1 unchanged. Refresh order platform → research → compare.
- No pair routes, no pair files; shells compose in the browser. Canonical = shell; no query URL in the sitemap.
- Matchup registry windows (MLB from 2026-09-17, NFL 2026 from Week 1) are fixed; the builder refuses drops; budget 600
  — ⚠ an MLB 2027 season will exceed it: decide a rolling archive policy before Opening Day 2027.
- Forecast on matchup pages is a LINK to the owner's report (+ PUBLISHED-range player names), never a value.
- `stat-families.mjs` is browser-bundled: it must never import `research-pages/stat-groups.mjs` (platform family keys
  leaked into a chunk once; EX1 + CX3).
- `public/data/compare/v1/` is gitignored, emitted each build; `compare/v1/` is the only runtime-assembled prune prefix.

## D. Defects found (classified)

| class | finding | disposition |
|---|---|---|
| product / a11y | `sr-only` header cell inside a non-positioned scroller escaped it → document 510 px on a 390 px phone (page zoomed out) on Team Compare + Matchup; also v1.3 research tables (389 px at 320) | scrollers `position: relative`; a11y spec pins document width for research/compare/matchup routes |
| projection / leak | client bundle carried `epl.espn-player-match` via stat-groups import (export-leak EX1) | cross-check moved to build-only module; CX3 pins |
| projection | season summaries/recent finals did not dedupe repeated game ids (H2H did) — caught by CP9 | `uniqueByGame` |
| test/probe | probes 6, 14 vacuous (fixtures); 22 missed (client code in shared chunk); 21 runner anchor bug | fixtures + pure `mlbForecastLink` + CB7 scans page-specific chunks; all re-probed CAUGHT |
| test/probe | own tests: SM1 regex matched `/compare/players/`; CB2 `pick` matched brand; CX3 control tripped B1 | fixed |
| performance | CI quality 19m53s → 21m14s | under the 22-min stop line; headroom 3m46s — optimize next |
| P0/P1 | none |

## E. Remaining gaps

| Gap | Sport | User impact | Owner / next |
|---|---|---|---|
| id-keyed final scores | EPL | no Team Compare / matchups | data program (founder: source) |
| comparable fight statistics | UFC | no fighter compare | founder gate (licence) |
| 2026 player logs | NFL | player compare ≤2025 | data program |
| box scores | MLB | captured categories only | data program |
| team rows lack seasonPhase / neutral host | NFL | preseason could count (0 today); neutral games in id order | projection |
| manual refresh | all | new games get matchup pages only after a refresh | ops decision |
| CI headroom 3m46s | all | next routes risk the 25-min timeout | optimize a11y walker / build |
| Team Compare browser a11y in CI | MLB/NFL | local QA only | when headroom allows |

## F. v1.5 readiness

| Research Lab capability | Ready? | Missing |
|---|---|---|
| richer team filters | partial | home/away + season exist in rows; phase/neutral host not carried |
| richer player filters | yes (NFL/EPL) | MLB box scores, NFL 2026 |
| game finder | partial | registry pattern exists; no historical game index/budget |
| season explorer | yes (MLB/NFL teams) | EPL results |
| query composition | no | needs a query contract + budget |
| saved research state | no | needs a new owner decision (no new localStorage key was added) |

**Next bounded program: v1.5 Research Lab. Not started.**
