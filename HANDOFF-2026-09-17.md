# GameTime Picks — Engineering Handoff (2026-09-17, v1.2 GAMETIME DATA PLATFORM, ~07:55 UTC)

Supersedes `HANDOFF-2026-09-16.md` (v1.1.x history stays there). For the next session at
`/Users/yashwantbalaji/Downloads/gametimepicks`: read this file, `docs/GAMETIME_DATA_PLATFORM.md` (the v1.2
architecture + runbook), `docs/GAMETIME_LIVE.md`, `docs/RETENTION_ARCHITECTURE.md`, and the memory index
(`gtp-v12-data-platform`). Charter unchanged: no paid data, no new provider, no wagers, no social posts, never promote a
REJECTED/STOP/PAUSED model, never `git add -A`, merge never rebase, batch `app/` pushes, never commit `vp/**` /
`HANDOFF-2026-08-20.md` / `docs/execution/CODEX_NFL_JOINT_V3_CONTRACT.md` / pasted master prompts.

## ⭐ NEXT SESSION — START HERE

1. Print UTC + ET; `git fetch origin`; fast-forward (bots push `main` constantly).
2. Verify by behaviour exactly as in `HANDOFF-2026-09-16.md` START HERE §4 (Live, personal pages, noindex).
3. Data Platform health (from `app/`): `node scripts/data-platform/validate.mjs` (must be OK) ·
   `node scripts/data-platform/build.mjs --all --check` (STALE is expected once bots add captures — refresh with
   `--all`, then `validate.mjs`, `parity.mjs`, review `git diff --stat data/internal/platform`, commit explicit paths).
4. Next program: **v1.3 Team + Player Research** (not started). Read `docs/GAMETIME_DATA_PLATFORM.md` §21–22 first.

## A. State

| item | value |
|---|---|
| shipped | `19ff1ad99` code/tests · `d520c4b01` store v1 + receipts · `ae80d7780` docs · `d588c2115` comment fix · `2a60e7708` reviewed-table proofs (NFL-8, EPL-5) |
| startup truth | 06:37Z: HEAD = origin = `1092cc2d5`; production served `59ef6c9f3` (descendant of `466f5cd4c`); CI `35186834483` success 19m37s |
| CI | quality-gate **`35194704083` on `2a60e7708` — success** (22m41s wall; unit 190 s · build 348 s · rendered 128 s · a11y 265 s; browser install 92 s). `35194625622`/`35194648732` read **cancelled** — superseded by the later pushes (concurrency cancel-in-progress), not failures |
| gate (local) | lint 0 · tsc 0 · unit **6,637 / 6,636 / 0 fail / 1 skipped** (68 s) · build 0 · post-build **559 / 556 / 0 fail / 3 skipped** · route inventory **77 routes, 0 findings** (only `generatedAt` moved — not committed) |
| production | serves **`2a60e7708` exactly** (builtAt 07:29:48Z). Smoke 07:51Z: `/`, `/today`, `/live`, `/saved`, `/following`, `/my`, `/mlb`, `/nfl`, `/epl`, `/ufc`, `/results`, an MLB game, an NFL game, an EPL match, a UFC bout → 200 · `/preview/live` 404 · MLB Live events · NFL `UNSUPPORTED_SPORT` · `/my` noindex, not in sitemap · 0 platform strings in public pages · no store path served |
| public product change | **none.** No route, UI, model, Live or Saved change; the platform has no consumer |
| research receipts | P317 engine-level shadow ACCUMULATING 15/200 (last 2026-09-16T14:23Z, next nightly-settle) · P305-F EPL totals shadow **not due** (first rows ~2026-09-17 21:00Z; `forward-totals/` still empty) · nfl-props-forward-shadow last run success · soccer-dc-shadow failed 09-16 (known ESPN soccer 400) — classified only, nothing adopted |
| verdict | **GAMETIME DATA PLATFORM v1.2 — FOUNDATION BUILT AND VERIFIED** |

## B. What v1.2 is

Internal, file-backed canonical sports graph: `app/src/lib/data-platform/` (pure `.mjs`), CLIs in
`app/scripts/data-platform/{build,validate,parity,inspect}.mjs`, store `data/internal/platform/v1` (schemaVersion 1,
gzip JSONL partitions by sport/season, manifest of uncompressed content hashes, no wall clock in content).

| Sport | Teams | Players | Games | Team-game | Player-game | Depth |
|---|---:|---:|---:|---:|---:|---|
| MLB | 30 | 732 | 8,868 | 16,242 | 19,483 (prop actuals, partial) | finals 2023–25 (7,289) + 2026 |
| NFL | 32 | 4,023 | 7,334 | 14,538 | 107,939 | finals 1999–2026; ESPN lines 2023–25; nflverse 2013–25 |
| EPL | 27 | 1,565 | 1,900 | — unsupported | 60,742 | 2022-23…2025-26 + 2026-27 fixtures |
| UFC | — | 1,085 | 1,812 | — | 3,478 | 2023-08…2026-11 |

Build 5.2 s (peak RSS ≈0.9 GB) · validate ≈1.1 s · 108.9 MB content / 3.7 MB stored · reads < 0.2 ms after a
17–284 ms lazy load · byte-identical fresh rebuild proven · parity 21 checks: 18 MATCH, 3 EXPLAINED (declared gaps),
0 UNEXPLAINED · 13 mutation probes caught (one probe defect found and fixed: a name-join probe that the precedence
table already neutralized).

## C. Decisions a later session must not undo silently

- Shipped ids reused verbatim; new ids: `mlb-player-<StatsAPI person id>` (INTERNAL — not a Follow id yet),
  `epl-team-/epl-athlete-/ufc-athlete-<ESPN id>`, EPL history games = ESPN event id, seasons `MLB-2025`/`NFL-2025`/
  `EPL-2025-26`/`UFC-2026`.
- **Guards that changed sides deliberately:** `mlb/finals-history-isolation.test.mjs` now lets ONLY the platform
  package read the 2023–25 archive — safe because `data-platform/boundary.test.mjs` B1 fails if anything else imports
  the package or reads the store. **A first v1.3 consumer must revisit both** (build-time projection only; never a
  live model input). `soccer/epl-closeout-guard.test.mjs` lists the platform as a cross-sport reader.
- `lib/sports/source-registry.mjs` now records **nflverse** (PRIVATE_RESEARCH; in use since P257-F, never registered).

## D. Defects found (classified)

| class | finding | disposition |
|---|---|---|
| Source data defect | nflverse games-history-v2: 11 ESPN ids claimed by 2–3 games (2003–2010, 23 rows) | dropped fail-closed; research consumers key by game_id, not affected |
| Source data defect | `data/internal/nfl/official-stats` zero-fills categories an athlete never appeared in | not normalized; declared slice. Harmless for its owner (grading) |
| Existing product identity hazard (P2) | shipped EPL event ids embed kickoff → 252 superseded ids | lineage aliases via openfootball key; changing the convention is a founder call (gate 3) |
| Identity ambiguity | ESPN keeps a UFC competition id when a fighter is replaced (284 changes across captures) | newest capture only; stale fighters never back-filled |
| Operational/data retention | NFL/UFC `results/latest.json` are rolling overwritten windows | 2026 finals outside the window are SOURCE_MISSING; retaining dated results captures would close it |
| Identity coverage | nflverse id bridge covers 2022+ players only | 34,427 nflverse rows unresolved, never minted |
| Probe defect | first name-join mutation changed no output (linescores have no rank for team sides) | strengthened to a real join; caught |
| Guard/test | EPL lane guard flagged the platform (post-build) | allowlisted as cross-sport reader with reason |
| P0/P1 | none |

## E. Gaps for v1.3 (full table: `docs/GAMETIME_DATA_PLATFORM.md` §21)

| Sport | Team pages | Player pages | Blocker |
|---|---|---|---|
| MLB | READY (runs only) | PARTIAL | no committed box scores; player identity only for prop-carrying players |
| NFL | READY (2026 finals window gap) | READY 2023–25 · PARTIAL 2013–22 · BLOCKED 2026 | zero-safe 2026 box-score capture; retained results |
| EPL | PARTIAL (no results) | READY 2022–26 · BLOCKED 2026-27 | id-keyed final scores; current-season lines |
| UFC | n/a | PARTIAL (win/loss only) | method/round source; results retention |

## F. Still open (unchanged from v1.1.4.2)

25-minute quality job margin and a11y/browser-install runner variance · untimed `npx tsx` launches in five other test
files · ESPN soccer scoreboard HTTP 400 scheduled refusals · founder: Live as a sixth primary, NFL provider posture.
No scheduled job refreshes the platform store (ops decision).

## G. Next bounded program

**v1.3 Team + Player Research** — build-time projections from `openPlatform` for team and player pages where the
readiness table says READY, with coverage-aware copy for every declared gap. Not started.
