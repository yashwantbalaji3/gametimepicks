{
  "_comment": "Manual playerId aliases for Odds-API names that don't match nba_api's static index. Format: 'name as it appears in Odds API' -> NBA.com canonical player id. Verify each id against https://www.nba.com/stats/player/<id>/ before adding. Empty by default; add entries only when player_resolver_test surfaces a confirmed miss for a real prop.",
  "aliases": {}
}
