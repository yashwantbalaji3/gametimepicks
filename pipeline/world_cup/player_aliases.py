"""Manual sportsbook→canonical player-name aliases (documented, never invented)."""
PLAYER_ALIASES = {
    # sportsbook normalized full-name → API-Football normalized name, only where token matching
    # is insufficient. Add entries with a source comment when needed.
}
