"""UFC pipeline package — FAIL-CLOSED scaffolding.

Mirrors app/src/lib/ufc-types.ts launch gates in Python. Produces only a
readiness artifact from REAL provider state; never emits picks/projections/parlays
until odds + fighter stats + results grading + a backtest all exist.
"""
